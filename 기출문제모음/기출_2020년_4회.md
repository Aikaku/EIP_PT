# 2020년 정보처리기사 실기 기출문제 — 4회

> 총 20문제

---

**Q01.** 현재 IPv4의 확장형으로 IPv4가 가지고 있는 주소 고갈, 보안성, 이동성 지원 등의 문제점을 해결하기 위해서 개발된 128비트 주소체계를 갖는 차세대 인터넷 프로토콜은 무엇인가?

---

**Q02.** 목적에 따른 디자인 패턴의 유형에는 생성(Creational) 패턴, 구조(Structural) 패턴, ( ) 패턴이 있다. 괄호에 들어갈 알맞은 패턴 유형을 쓰시오.

---

**Q03.** 다음은 어떤 UML 다이어그램에 관한 예시이다. 어떤 종류의 다이어그램인가?

```
[사용자]  [시스템]
   |          |
   |--요청--→ |
   |          |
   |←--응답-- |
   |          |
```

*(객체 간 메시지 교환 순서와 흐름을 시간의 흐름에 따라 표현)*

---

**Q04.** 데이터베이스의 회복(Recovery) 기법 중 Rollback 시 Redo, Undo가 모두 실행되는 트랜잭션 처리법으로 트랜잭션 수행 중 갱신 결과를 바로 DB에 반영하는 기법은 무엇인가?

---

**Q05.** 다음은 n이 10일 때, 10을 2진수로 변환하는 자바 소스 코드이다. A, B 괄호 안에 알맞은 값을 적으시오.

```java
class good {
    public static void main(String[] args) {
        int[] a = new int[8];
        int i = 0;
        int n = 10;
        while ( A ) {
            a[i++] = ( B );
            n /= 2;
        }
        for (i = 7; i >= 0; i--) {
            System.out.print(a[i]);
        }
    }
}
```

---

**Q06.** 다음은 자바 소스 코드이다. 출력 결과를 보고 괄호 A, B에 알맞은 값을 적으시오.

```java
public class good {
    public static void main(String[] args) {
        int[][] a = new int[( A )][( B )];
        for (int i = 0; i < 3; i++) {
            for (int j = 0; j < 5; j++) {
                a[i][j] = j * 3 + (i + 1);
                System.out.print(a[i][j] + " ");
            }
            System.out.println();
        }
    }
}
```

[출력 결과]
```
1 4 7 10 13
2 5 8 11 14
3 6 9 12 15
```

---

**Q07.** 해킹 공격의 종류 중 하나인 스니핑(Sniffing)에 대하여 설명하시오.

---

**Q08.** IP 패킷에서 외부의 공인 IP주소와 포트 주소에 해당하는 내부 IP주소를 재기록하여 라우터를 통해 네트워크 트래픽을 주고받는 기술을 무엇이라고 하는가?

---

**Q09.** 다음은 파이썬 소스 코드이다. 출력 결과를 쓰시오.

```python
lol = [[1, 2, 3], [4, 5], [6, 7, 8, 9]]
print(lol[0])
print(lol[2][1])
for sub in lol:
    for item in sub:
        print(item, end='')
    print()
```

---

**Q10.** 분산 컴퓨팅 기술 기반의 데이터 위변조 방지 기술로 P2P 방식을 기반으로 하여 소규모 데이터들이 연결되어 형성된 '블록'이라는 분산 데이터 저장 환경에 관리 대상 데이터를 저장함으로써 누구도 임의로 수정할 수 없고 누구나 변경의 결과를 열람할 수 있게끔 만드는 기술은 무엇인가?

---

**Q11.** 오픈 소스 기반으로 한 분산 컴퓨팅 플랫폼으로, 일반 PC급 컴퓨터들로 가상화된 대형 스토리지를 형성하고 그 안에 보관된 거대한 데이터 세트를 병렬로 처리할 수 있도록 개발된 자바 소프트웨어 프레임워크로 구글, 야후 등에 적용한 기술은 무엇인가?

---

**Q12.** 데이터베이스 이상 현상(Anomaly)의 종류 3가지를 쓰시오.

---

**Q13.** 다음은 프로세스 상태 전이도이다. ①, ②, ③에 알맞은 상태를 쓰시오.

```
          디스패치(Dispatch)
    ①  ─────────────────→  ②
  (  )  ←─────────────────  (  )
          CPU 선점(Preemption)
           |                  |
           |입력 완료          |입출력/이벤트
           ↓                  ↓
          생성 ──────────→  ③(  )
```

*(프로세스의 일반적인 상태: 생성, 준비, 실행, 대기, 완료)*

---

**Q14.** 테스트 오라클 중 특정한 몇 개의 입력값에 대해서만 기대하는 결과를 제공해주는 오라클은 무엇인가?

---

**Q15.** 점수에 따른 성적 부여가 잘 되었는지 테스트하고자 한다. 아래에 알맞는 테스트 기법은 무엇인가?

| 점수 | 성적 |
|------|------|
| 0 ~ 59 | 가 |
| 60 ~ 69 | 양 |
| 70 ~ 79 | 미 |
| 80 ~ 89 | 우 |
| 90 ~ 100 | 수 |

[테스트 값] : -10점 / 30점 / 65점 / 75점 / 85점 / 95점 / 110점

---

**Q16.** 다음 조건을 만족하면서 학과별로 튜플 수가 얼마인지 구하는 SQL문을 작성하시오.

- WHERE 구문을 사용하지 않는다.
- GROUP BY 를 사용한다.
- 별칭(AS)을 사용한다.
- 집계 함수를 사용한다.

[학생]

| 학생 | 학과 |
|------|------|
| 이순신 | 컴퓨터 |
| 안중근 | 컴퓨터 |
| 윤봉길 | 전자 |
| 이봉창 | 전자 |
| 강우규 | 전기 |

[결과]

| 학과 | 학과별튜플수 |
|------|------------|
| 전기 | 1 |
| 컴퓨터 | 2 |
| 전자 | 2 |

---

**Q17.** 데니스 리치와 켄 톰슨 등이 함께 벨 연구소를 통해 만든 운영체제이며, 90% 이상 C언어로 구현되어 있고, 시스템 프로그램이 모듈화되어 있어서 다른 하드웨어 기종으로 쉽게 이식 가능하며 계층적 트리 구조를 가짐으로써 통합적인 파일 관리가 용이한 운영체제는 무엇인가?

---

**Q18.** 다음은 C언어 소스 코드이다. 출력 값을 쓰시오.

```c
#include <stdio.h>
int main() {
    char *p = "KOREA";
    printf("%s\n", p);
    printf("%s\n", p + 3);
    printf("%c\n", *p);
    printf("%c\n", *(p + 3));
    printf("%c\n", *p + 2);
    return 0;
}
```

---

**Q19.** 다음은 자바(Java) 소스 코드이다. 출력 결과를 쓰시오.

```java
class Parent {
    public int compute(int num) {
        if (num <= 1) return num;
        return compute(num - 1) + compute(num - 2);
    }
}
class Child extends Parent {
    public int compute(int num) {
        if (num <= 1) return num;
        return compute(num - 1) + compute(num - 3);
    }
}
class Good {
    public static void main(String[] args) {
        Parent obj = new Child();
        System.out.print(obj.compute(4));
    }
}
```

---

**Q20.** 정보 보안의 3요소 중 가용성(Availability)에 대하여 설명하시오.

---
---

## 정답 모음

| 번호 | 정답 | 풀이 |
|------|------|------|
| Q01 | IPv6 (Internet Protocol version 6) | IPv4(32비트) → IPv6(128비트). 주소 고갈 해결, IPSec 기본 내장으로 보안 강화, 이동성 지원 |
| Q02 | 행위(Behavioral) | GoF 패턴 3분류: 생성(객체 생성 방법), 구조(클래스·객체 구성), **행위(객체 간 상호작용·책임 분배)** |
| Q03 | 시퀀스 다이어그램(Sequence Diagram) | UML 동적 다이어그램. 객체 간 메시지 교환을 시간 흐름(수직 방향)에 따라 표현 |
| Q04 | 즉시 갱신(Immediate Update) 기법 | 트랜잭션 수행 중 변경 내용을 즉시 DB에 반영. 장애 시 Undo(미완료 롤백) + Redo(완료 재실행) 모두 필요 |
| Q05 | A: `n > 0` / B: `n % 2` | n=10 → 10%2=0, n=5 → 5%2=1, n=2 → 2%2=0, n=1 → 1%2=1, n=0 종료. 배열 역순 출력 → 00001010 |
| Q06 | A: `3` / B: `5` | i 루프 3회(0~2), j 루프 5회(0~4). a[i][j]=j×3+(i+1) → 출력 결과 일치. 배열 크기 = new int[3][5] |
| Q07 | 네트워크 상에서 자신이 아닌 다른 상대방들의 패킷 교환을 엿듣는(도청하는) 행위 | 수동적 공격. 네트워크 트래픽을 캡처해 개인정보·계정 탈취. 방어책: 암호화(SSL/TLS) |
| Q08 | NAT (Network Address Translation) | 사설 IP ↔ 공인 IP 변환. 내부 네트워크 구조 은닉 + IPv4 주소 절약 효과 |
| Q09 | `[1, 2, 3]` / `7` / `123` / `45` / `6789` | lol[0]=[1,2,3] 리스트 출력, lol[2][1]=7, 이중 for문으로 각 리스트 원소 이어붙여 출력 후 줄바꿈 |
| Q10 | 블록체인(Blockchain) | P2P 분산 원장 기술. 블록(데이터 묶음)을 체인으로 연결. 한 블록 변조 시 전체 체인 재계산 필요 → 위변조 사실상 불가 |
| Q11 | 하둡(Hadoop) | 아파치 오픈소스. MapReduce 분산 처리 + HDFS 분산 파일 시스템. 대용량 데이터 병렬 처리 |
| Q12 | 삽입 이상(Insertion Anomaly), 삭제 이상(Deletion Anomaly), 갱신 이상(Update Anomaly) | 정규화가 안 된 테이블에서 발생. 정규화로 해결. 삽입=불필요 데이터 함께 삽입, 삭제=관련 없는 데이터 함께 삭제, 갱신=일부만 수정되어 불일치 |
| Q13 | ① 준비(Ready) / ② 실행(Running) / ③ 대기(Blocked/Waiting) | 프로세스 상태 전이: 생성→준비→(디스패치)→실행→(I/O발생)→대기→(완료)→준비. CPU 선점 시 실행→준비 |
| Q14 | 샘플링 오라클(Sampling Oracle) | 테스트 오라클 4종: 참(전수 검사), **샘플링(일부만 검사)**, 휴리스틱(경험 기반), 일관성(버전 간 비교) |
| Q15 | 동치 분할(Equivalence Partitioning) 테스트 | 입력 도메인을 동등한 구간(클래스)으로 나눠 구간별 대표값으로 테스트. -10·110은 유효 범위 밖, 나머지는 각 성적 구간 대표값 |
| Q16 | `SELECT 학과, COUNT(*) AS 학과별튜플수 FROM 학생 GROUP BY 학과;` | GROUP BY로 학과별 그룹화, COUNT(*)로 튜플 수 집계, AS로 별칭 부여, WHERE 없이 전체 대상 |
| Q17 | UNIX | 1969년 벨 연구소. C언어 90% 구현으로 이식성↑. 계층적 트리 파일 구조. 멀티유저·멀티태스킹 지원 |
| Q18 | `KOREA` / `EA` / `K` / `E` / `M` | p="KOREA": %s→문자열 전체, p+3=%s→'E'부터="EA", *p=%c→'K', *(p+3)=%c→'E', *p+2='K'(75)+2=77='M' |
| Q19 | `1` | 다형성: new Child()이므로 Child.compute 실행. compute(2)=compute(1)+compute(-1)=1+(-1)=0, compute(3)=0+0=0, compute(4)=0+1=1 |
| Q20 | 인가된 사용자는 필요할 때 언제든지 데이터에 접근하고 사용할 수 있어야 한다는 보안 속성 | 정보 보안 3요소(CIA): 기밀성(Confidentiality, 허가된 자만 접근), 무결성(Integrity, 변조 방지), **가용성(Availability, 필요 시 사용 가능)** |
