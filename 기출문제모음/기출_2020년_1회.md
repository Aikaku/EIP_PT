# 2020년 정보처리기사 실기 기출문제 — 1회

> 총 20문제

---

**Q01.** 다음 ( ) 안에 들어갈 단어를 쓰시오.

( )은(는) 웹브라우저 간 HTML 문법이 호환되지 않는 문제와 SGML의 복잡함을 해결하기 위하여 개발된 다목적 마크업 언어이다.

---

**Q02.** 다음 ( ) 안에 들어갈 단어를 쓰시오.

( )은 속성-값 쌍(attribute-value pairs)으로 이루어진 데이터 오브젝트를 전달하기 위해 사용하는 개방형 표준 포맷이다. AJAX에서 많이 사용되고 XML을 대체하는 주요 데이터 포맷이다. 언어 독립형 데이터 포맷으로 다양한 프로그래밍 언어에서 사용되고 있다.

---

**Q03.** 다음은 릴리즈 노트의 구성 항목에 관한 설명이다. 설명하는 항목은 무엇인가?

릴리즈 노트 이름, 소프트웨어 이름, 릴리즈 버전, 릴리즈 날짜, 릴리즈 노트 날짜, 릴리즈 노트 버전 등의 기존 정보가 포함된다.

---

**Q04.** 애플리케이션 테스트의 기본 원리 중 살충제 패러독스(Pesticide Paradox)의 의미를 설명하시오.

---

**Q05.** 데이터 마이닝(Data Mining)이란 무엇인지 설명하시오.

---

**Q06.** 프로토콜을 구성하는 대표적인 세 가지 요소를 적으시오.

---

**Q07.** 다음에서 설명하는 해시 함수는?

128비트 암호화 해시 함수로 RFC 1321로 지정되어 있으며, 주로 프로그램이나 파일이 원본 그대로인지를 확인하는 무결성 검사 등에 사용된다. 1991년에 로널드 라이베스트(Ronald Rivest)가 예전에 쓰이던 MD4를 대체하기 위해 고안하였다.

---

**Q08.** 비선점형 스케줄링 HRN의 우선순위 계산식을 작성하시오.

---

**Q09.** 아래는 데이터베이스 트랜잭션의 4가지 속성이다. (가), (나)에 들어갈 단어를 적으시오.

| 속성 | 설명 |
|------|------|
| **(가)** | 트랜잭션은 연산들을 전부 실행하든지 전혀 실행하지 않아야 한다. 일부만 실행해서는 안 된다. |
| 일관성 | 트랜잭션이 성공적으로 실행되면 데이터베이스 상태는 모순되지 않고 일관된 상태가 된다. |
| **(나)** | 트랜잭션 실행 도중의 연산 결과는 다른 트랜잭션에서 접근할 수 없다. |
| 지속성 | 트랜잭션이 성공했을 경우 영구적으로 반영되어야 한다. |

---

**Q10.** 다음에서 설명하는 서비스 거부 공격은?

패킷의 출발지 주소(Address)나 포트(port)를 임의로 변경하여 출발지와 목적지 주소(또는 포트)를 동일하게 함으로써, 공격 대상 컴퓨터의 실행 속도를 느리게 하거나 동작을 마비시켜 서비스 거부 상태에 빠지도록 하는 공격

---

**Q11.** OSI 7계층 중 다음에서 설명하는 계층은?

전송에 필요한 두 장치 간의 실제 접속과 절단 등 기계적, 전기적, 기능적, 절차적 특성에 대한 규칙을 정의한다. 단위(PDU)는 '비트'를 사용한다.

---

**Q12.** LoC 기법으로 개발을 해야 하는 프로젝트의 총 라인이 30,000 라인이고, 개발자가 5명, 인당 월평균 300라인의 개발이 가능할 때, 개발하는데 소요될 것으로 예상되는 시간의 계산식과 결과를 작성하시오.

---

**Q13.** 아래는 애플리케이션 성능을 측정하기 위한 요소들이다. (가), (나), (다)에 들어갈 단어를 적으시오.

| 속성 | 설명 |
|------|------|
| **(가)** | 일정 시간 내에 애플리케이션이 처리하는 일의 양 |
| **(나)** | 애플리케이션에 요청을 전달한 시간부터 응답이 도착할 때까지 걸린 시간 |
| **(다)** | 애플리케이션에 작업을 의뢰한 시간부터 처리가 완료될 때까지 걸린 시간 |
| 자원 사용률 | 애플리케이션이 처리하는 동안의 CPU 사용량, 메모리 사용량, 네트워크 사용량 등 |

---

**Q14.** 다음은 소프트웨어 모듈화에 관한 설명이다. (가), (나)에 들어갈 단어를 각각 적으시오.

모듈의 기능적 독립성을 높이기 위해서는 **(가)** 는 약하게, **(나)** 는 강하게 만들어야 한다.

---

**Q15.** 데이터베이스 성능 개선과 관련하여 '데이터베이스 반정규화'란 무엇인지 설명하시오.

---

**Q16.** 다음의 시스템 구조도에서 팬인(Fan-in)이 2 이상인 것은?

```
        A
      / | \
     B  C  D
    /\ /|\ /\
   E  F   G  H
      |   |
      I   J
```

(F는 B, C에 의해 호출 / J는 G, H에 의해 호출)

---

**Q17.** 다음은 C언어로 작성된 코드이다. 실행 결과를 쓰시오.

```c
#include <stdio.h>
void align(int a[]) {
    int temp;
    for (int i = 0; i < 4; i++) {
        for (int j = 0; j < 4 - i; j++) {
            if (a[j] > a[j+1]) {
                temp = a[j];
                a[j] = a[j+1];
                a[j+1] = temp;
            }
        }
    }
}
main() {
    int a[] = { 85, 75, 50, 100, 95 };
    align(a);
    for (int i = 0; i < 5; i++) printf("%d ", a[i]);
}
```

---

**Q18.** 다음은 C언어로 작성된 코드이다. 실행 결과를 쓰시오.

```c
#include <stdio.h>
main() {
    int c = 1;
    switch (3) {
        case 1: c += 3;
        case 2: c++;
        case 3: c = 0;
        case 4: c += 3;
        case 5: c -= 10;
        default: c--;
    }
    printf("%d", c);
}
```

---

**Q19.** 다음은 Java언어로 작성된 코드이다. 실행 결과를 쓰시오.

```java
public class Test {
    static int[] arr() {
        int a[] = new int[4];
        int b = a.length;
        for(int i = 0; i < b; i++)
            a[i] = i;
        return a;
    }
    public static void main(String[] args) {
        int a[] = arr();
        for(int i = 0; i < a.length; i++)
            System.out.print(a[i] + " ");
    }
}
```

---

**Q20.** 학생 테이블에 전기과 학생이 50명, 전산과 학생이 100명, 전자과 학생이 50명 있다고 할 때, 다음 SQL문 ①, ②, ③의 실행 결과로 표시되는 튜플의 수를 쓰시오. (단, DEPT 필드는 학과를 의미)

```sql
① SELECT 학과 FROM 학생;
② SELECT DISTINCT 학과 FROM 학생;
③ SELECT COUNT(DISTINCT 학과) FROM 학생 WHERE 학과='전산과';
```

---
---

## 정답 모음

| 번호 | 정답 | 풀이 |
|------|------|------|
| Q01 | XML (eXtensible Markup Language) | 브라우저 간 HTML 호환 문제 + SGML 복잡함 해결을 위한 다목적 마크업 언어 |
| Q02 | JSON (JavaScript Object Notation) | 속성-값 쌍 구조의 경량 데이터 포맷. AJAX에서 XML 대체재로 주로 사용 |
| Q03 | 헤더(Header) | 릴리즈 노트 구성 항목 중 이름·버전·날짜 등 기본 식별 정보를 담는 부분 |
| Q04 | 동일한 테스트 케이스로 반복 테스트를 수행하면 더 이상 결함이 발견되지 않는 현상 | 살충제에 내성이 생기듯, 테스트 케이스를 주기적으로 추가·변경해야 새로운 결함 발견 가능 |
| Q05 | 대규모로 저장된 데이터 안에서 체계적이고 자동적으로 통계적 규칙이나 패턴을 분석하여 가치 있는 정보를 추출하는 과정 | 빅데이터에서 숨겨진 패턴·관계를 통계적 기법으로 자동 발굴하는 기술 |
| Q06 | 구문(Syntax), 의미(Semantics), 시간(Timing) | 구문=데이터 형식·구조, 의미=데이터의 뜻·해석, 시간=전송 순서·속도·동기화 |
| Q07 | MD5 | 128비트 해시 함수, RFC 1321, 1991년 Ronald Rivest가 MD4 대체용으로 개발. 무결성 검사에 사용 |
| Q08 | (대기 시간 + 서비스 시간) / 서비스 시간 | 대기가 길수록 우선순위↑ → 기아 현상(Starvation) 방지. 비선점형 스케줄링 |
| Q09 | (가) 원자성(Atomicity) / (나) 고립성(Isolation) | ACID 4속성: 원자성=전부 or 전무, 일관성=모순 없음, 고립성=다른 트랜잭션 차단, 지속성=영구 반영 |
| Q10 | LAND 공격 | 출발지 IP·포트를 목적지와 동일하게 변조 → 자기 자신에게 응답 반복 → CPU 과부하·마비 |
| Q11 | 물리 계층(Physical Layer, 1계층) | OSI 최하위 계층. PDU=비트. 케이블·허브 등 전기적·기계적 신호 전송 규칙 정의 |
| Q12 | 계산식: 30,000 / (5 × 300) = **20개월** | 총 라인 수 ÷ (개발자 수 × 월 생산성) = 30,000 ÷ 1,500 = 20 |
| Q13 | (가) 처리량(Throughput) / (나) 응답 시간(Response Time) / (다) 경과 시간(Turn Around Time) | 처리량=단위시간당 처리량, 응답시간=요청→첫 응답, 경과시간=요청→처리 완료 |
| Q14 | (가) 결합도(Coupling) / (나) 응집도(Cohesion) | 결합도↓ + 응집도↑ = 모듈 독립성↑ → 유지보수·재사용 용이 |
| Q15 | 데이터베이스 성능 향상을 위해 정규화된 데이터 모델에서 의도적으로 중복을 허용하고 조인을 줄이는 기법 | 정규화(중복 제거·이상 현상 방지) ↔ 반정규화(조회 성능 향상 위해 중복 허용) |
| Q16 | F, J | F는 B·C 두 모듈에서 호출(팬인=2), J는 G·H 두 모듈에서 호출(팬인=2) |
| Q17 | `50 75 85 95 100` | 버블 정렬: {85,75,50,100,95} → 인접 원소 비교·교환 반복 → 오름차순 정렬 |
| Q18 | `-8` | case 3 매칭 후 break 없어 fall-through: c=0 → c+=3(=3) → c-=10(=-7) → c--( =-8) |
| Q19 | `0 1 2 3` | int 배열 기본값 0 초기화. a[i]=i로 0,1,2,3 저장 후 공백 포함 순서 출력 |
| Q20 | ① 200 / ② 3 / ③ 1 | ① 전체 학생 200명, ② DISTINCT 학과 3종류, ③ WHERE 전산과 조건 후 DISTINCT 학과 = 1 |
