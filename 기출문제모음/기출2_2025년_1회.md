# 2025년 정보처리기사 실기 기출문제 — 1회

> 총 20문제

---

**Q01.** 다음 Java 코드에서 알맞은 출력 값을 작성하시오.

```java
public class Main {
    public static void main(String[] args) {
        int a = 5, b = 0;
        try {
            System.out.print(a / b);
        } catch (ArithmeticException e) {
            System.out.print("1");
        } catch (ArrayIndexOutOfBoundsException e) {
            System.out.print("2");
        } catch (NumberFormatException e) {
            System.out.print("3");
        } catch (Exception e) {
            System.out.print("4");
        } finally {
            System.out.print("5");
        }
    }
}
```

---

**Q02.** 다음 C언어 코드에서 알맞은 출력 값을 작성하시오.

```c
#include <stdio.h>
char Data[5] = {'B', 'A', 'D', 'E'};
char c;
int main() {
    int i, temp, temp2;
    c = 'C';
    printf("%d\n", Data[3] - Data[1]);
    for (i = 0; i < 5; ++i) {
        if (Data[i] > c) break;
    }
    temp = Data[i];
    Data[i] = c;
    i++;
    for (; i < 5; ++i) {
        temp2 = Data[i];
        Data[i] = temp;
        temp = temp2;
    }
    for (i = 0; i < 5; i++) {
        printf("%c", Data[i]);
    }
}
```

---

**Q03.** 다음 C언어 코드에서 알맞은 출력 값을 작성하시오.

```c
#include <stdio.h>
#include <stdlib.h>
void set(int** arr, int* data, int rows, int cols) {
    for (int i = 0; i < rows * cols; ++i) {
        arr[((i + 1) / rows) % rows][(i + 1) % cols] = data[i];
    }
}
int main() {
    int rows = 3, cols = 3, sum = 0;
    int data[] = {5, 2, 7, 4, 1, 8, 3, 6, 9};
    int** arr;
    arr = (int**)malloc(sizeof(int*) * rows);
    for (int i = 0; i < cols; i++) {
        arr[i] = (int*)malloc(sizeof(int) * cols);
    }
    set(arr, data, rows, cols);
    for (int i = 0; i < rows * cols; i++) {
        sum += arr[i / rows][i % cols] * (i % 2 == 0 ? 1 : -1);
    }
    for (int i = 0; i < rows; i++) free(arr[i]);
    free(arr);
    printf("%d", sum);
}
```

---

**Q04.** 다음 Java 코드에서 알맞은 출력 값을 작성하시오.

```java
public class Main {
    public static void main(String[] args) {
        new Child();
        System.out.println(Parent.total);
    }
}
class Parent {
    static int total = 0;
    int v = 1;
    public Parent() {
        total += (++v);
        show();
    }
    public void show() {
        total += total;
    }
}
class Child extends Parent {
    int v = 10;
    public Child() {
        v += 2;
        total += v++;
        show();
    }
    @Override
    public void show() {
        total += total * 2;
    }
}
```

---

**Q05.** 다음 코드에서 루프 조건의 논리적 오류(AND 대신 OR 사용)가 나타나 배열 경계를 초과할 수 있다. 이 조건의 취약점을 수정하여 올바른 조건을 작성하시오.

```c
int Main(int b[], int m, int x) {
    int a = 0;
    while (a < m || b[a] < x) {
        if (b[a] < 0)
            b[a] = -b[a];
        a++;
    }
    return 1;
}
```

*(조건 `a < m || b[a] < x`에서 OR 조건으로 인해 a가 m 이상이어도 b[a]를 접근할 수 있어 배열 경계 오류 발생)*

---

**Q06.** 다음 Java 코드에서 알맞은 출력 값을 작성하시오.

```java
public class Main {
    public static void main(String[] args) {
        int[] data = {3, 5, 8, 12, 17};
        System.out.println(func(data, 0, data.length - 1));
    }
    static int func(int[] a, int st, int end) {
        if (st >= end) return 0;
        int mid = (st + end) / 2;
        return a[mid] + Math.max(func(a, st, mid), func(a, mid + 1, end));
    }
}
```

---

**Q07.** 다음 파이썬 코드에서 알맞은 출력 값을 작성하시오.

```python
class Node:
    def __init__(self, value):
        self.value = value
        self.children = []

def tree(li):
    nodes = [Node(i) for i in li]
    for i in range(1, len(li)):
        nodes[(i - 1) // 2].children.append(nodes[i])
    return nodes[0]

def calc(node, level=0):
    if node is None:
        return 0
    return (node.value if level % 2 == 1 else 0) + sum(calc(n, level + 1) for n in node.children)

li = [3, 5, 8, 12, 15, 18, 21]
root = tree(li)
print(calc(root))
```

---

**Q08.** 다음 C언어 코드에서 알맞은 출력 값을 작성하시오.

```c
#include <stdio.h>
#include <stdlib.h>
typedef struct Data {
    int value;
    struct Data* next;
} Data;

Data* insert(Data* head, int value) {
    Data* new_node = (Data*)malloc(sizeof(Data));
    new_node->value = value;
    new_node->next = head;
    return new_node;
}
Data* reconnect(Data* head, int value) {
    if (head == NULL || head->value == value) return head;
    Data *prev = NULL, *curr = head;
    while (curr != NULL && curr->value != value) {
        prev = curr;
        curr = curr->next;
    }
    if (curr != NULL && prev != NULL) {
        prev->next = curr->next;
        curr->next = head;
        head = curr;
    }
    return head;
}
int main() {
    Data *head = NULL, *curr;
    for (int i = 1; i <= 5; i++)
        head = insert(head, i);
    head = reconnect(head, 3);
    for (curr = head; curr != NULL; curr = curr->next)
        printf("%d", curr->value);
    return 0;
}
```

---

**Q09.** 다음 C언어 코드에서 알맞은 출력 값을 작성하시오.

```c
#include <stdio.h>
typedef struct student {
    char* name;
    int score[3];
} Student;

int dec(int enc) {
    return enc & 0xA5;
}
int sum(Student* p) {
    return dec(p->score[0]) + dec(p->score[1]) + dec(p->score[2]);
}
int main() {
    Student s[2] = {
        "Kim", {0xA0, 0xA5, 0xDB},
        "Lee", {0x90, 0xED, 0x81}
    };
    int result = 0;
    for (int i = 0; i < 2; i++) {
        result += sum(&s[i]);
    }
    printf("%d", result);
    return 0;
}
```

---

**Q10.** 다음 Java 코드에서 알맞은 출력 값을 작성하시오.

```java
public class Main {
    public static void main(String[] args) {
        System.out.println(calc("5"));
    }
    static int calc(int value) {
        if (value <= 1) return value;
        return calc(value - 1) + calc(value - 2);
    }
    static int calc(String str) {
        int value = Integer.valueOf(str);
        if (value <= 1) return value;
        return calc(value - 1) + calc(value - 3);
    }
}
```

---

**Q11.** 다음에서 설명하는 디자인 패턴을 작성하시오.

원래의 객체 대신 대리인(Proxy) 역할을 하는 객체를 두어 간접적으로 원래 객체를 접근하게 하는 패턴이다. 접근 제어, 로깅, 캐싱, 지연 초기화 등의 목적으로 사용된다.

---

**Q12.** 다음에서 설명하는 소프트웨어 테스트 관련 용어를 작성하시오.

테스트 실행 자동화를 위한 소프트웨어로, 다양한 테스트 도구들을 통합하여 테스트 환경을 구성하고 테스트 실행 및 결과를 관리하는 프레임워크이다. 테스트 드라이버, 테스트 스텁, 테스트 케이스 등을 포함한다.

---

**Q13.** 다음에서 설명하는 용어를 작성하시오.

여러 개의 독립적인 소형 서비스로 구성된 애플리케이션 아키텍처로, 각 서비스는 독립적으로 개발, 배포, 확장될 수 있다. 서비스 간 통신은 API를 통해 이루어진다.

---

**Q14.** 다음 SQL 문장에서 빈칸에 들어갈 알맞은 내용을 작성하시오.

학생 테이블에서 학과별 평균 성적이 80점 이상인 학과와 평균 성적을 구하시오.

```sql
SELECT 학과, AVG(성적) AS 평균성적
FROM 학생
GROUP BY 학과
( ) AVG(성적) >= 80;
```

---

**Q15.** 다음에서 설명하는 네트워크 프로토콜을 작성하시오.

- 인터넷 표준 프로토콜 중 하나로 네트워크 장치에서 오류 메시지를 전달하는 데 사용된다.
- ping 명령어에서 사용되는 Echo Request/Reply 메시지를 포함한다.
- IP 프로토콜 위에서 동작하지만 전송 계층이 아닌 네트워크 계층에 속한다.

---

**Q16.** 데이터베이스에서 트랜잭션의 ACID 특성 중 다음에서 설명하는 특성을 작성하시오.

하나의 트랜잭션이 완료되면, 그 결과는 영구적으로 저장되어야 하며, 시스템 장애가 발생하더라도 데이터가 손실되지 않아야 한다.

---

**Q17.** 다음에서 설명하는 SW 개발 방법론을 작성하시오.

짧은 개발 주기(스프린트)를 반복하며 실행 가능한 소프트웨어를 지속적으로 개발하는 방법론으로, 제품 백로그, 스프린트 계획, 일일 스크럼, 스프린트 리뷰 등의 이벤트로 구성된다.

---

**Q18.** 다음에서 설명하는 보안 공격을 작성하시오.

공격자가 DNS 서버의 캐시를 조작하여 특정 도메인 이름을 잘못된 IP 주소로 매핑시키는 공격이다. 사용자는 정상적인 URL을 입력했지만 공격자가 설정한 가짜 서버로 연결된다.

---

**Q19.** 다음에서 설명하는 SQL 명령어를 작성하시오.

테이블에 새로운 컬럼을 추가하거나 기존 컬럼을 수정, 삭제하는 DDL 명령어이다. 기존 테이블 구조를 변경하는 데 사용된다.

---

**Q20.** 다음에서 설명하는 테스트 기법을 작성하시오.

두 소프트웨어 버전의 출력을 비교하여 변경 사항이 의도치 않은 부작용을 일으키지 않는지 검증하는 테스트이다. 소프트웨어가 수정된 후에도 기존 기능이 정상적으로 작동하는지 확인한다.

---
---

## 정답 모음

| 번호 | 정답 | 풀이 |
|------|------|------|
| Q01 | 15 | 5/0 → ArithmeticException → "1" 출력. finally 항상 실행 → "5" 출력. 합=15 |
| Q02 | 4\nBACDE | Data[3]-Data[1]='E'-'A'=4. 'C'삽입 위치: Data[2]='D'>'C'→break. C 삽입 후 이동: BACDE |
| Q03 | 13 | set()으로 3×3 배열 채운 후 교번 부호 합산. 홀수인덱스 음수, 짝수 양수로 계산하면 sum=13 |
| Q04 | 54 | new Child(): Parent()→total=0+2=2, show()=Child.show()→total=2+4=6. Child(): v=10+2=12, total=6+12=18, v++=12→v=13, show()→total=18+36=54 |
| Q05 | a < m && b[a] < x | OR 조건 → a≥m이어도 b[a] 접근 가능(배열 경계 초과). AND로 바꿔야 a<m일 때만 b[a] 검사 |
| Q06 | 20 | func([3,5,8,12,17],0,4): mid=2,a[2]=8. max(func(0,2),func(3,4))=max(8,12)=12. 8+12=20 |
| Q07 | 13 | tree: [3]가 루트, level0. level1(홀수)에서 값 합산: 5+8=13. level2(짝수)=0 |
| Q08 | 35421 | insert 1~5 prepend: head=5→4→3→2→1. reconnect(3): 3을 앞으로 이동 → 3→5→4→2→1 |
| Q09 | 876 | dec(enc)=enc&0xA5. Kim:{0xA0,0xA5,0xDB}→160+165+129=454. Lee:{0x90,0xED,0x81}→128+165+129=422. 454+422=876 |
| Q10 | 4 | calc("5"): value=5, return calc(4)+calc(2)=3+1=4. calc(int)은 피보나치: calc(2)=1,calc(3)=2,calc(4)=3 |
| Q11 | Proxy (프록시) 패턴 | 구조 패턴. 실제 객체에 대한 대리인 역할. 접근 제어·캐싱·로깅 등에 활용 |
| Q12 | 테스트 하네스 (Test Harness) | 테스트 자동화 프레임워크. 드라이버·스텁·테스트 케이스를 통합 관리하는 소프트웨어 |
| Q13 | 마이크로서비스 아키텍처 (MSA) | 작고 독립적인 서비스들의 집합. 각 서비스는 독립 배포·확장. API 기반 통신 |
| Q14 | HAVING | GROUP BY 이후 조건 필터링에는 WHERE 대신 HAVING 사용 |
| Q15 | ICMP (Internet Control Message Protocol) | 네트워크 오류 메시지 전달 프로토콜. ping(Echo Request/Reply), traceroute에 사용. 네트워크 계층 |
| Q16 | 지속성 (Durability) | ACID: 원자성(A)·일관성(C)·고립성(I)·지속성(D). 지속성=커밋 후 영구 저장, 장애에도 손실 없음 |
| Q17 | 스크럼 (Scrum) | 애자일 방법론. 스프린트(1-4주) 반복. 제품 백로그→스프린트 백로그→일일 스크럼→스프린트 리뷰 |
| Q18 | DNS 스푸핑 (DNS Spoofing) / DNS 캐시 포이즈닝 | DNS 캐시 조작으로 도메인→잘못된 IP 연결. 사용자를 피싱 사이트로 유도 |
| Q19 | ALTER TABLE | DDL(Data Definition Language). 테이블 구조 변경: 컬럼 추가(ADD), 수정(MODIFY/ALTER), 삭제(DROP COLUMN) |
| Q20 | 회귀 테스트 (Regression Test) | 코드 변경 후 기존 기능 정상 작동 여부 확인. 버전 비교를 통해 부작용 검출 |
