# 2023년 정보처리기사 실기 기출문제 — 3회

> 총 20문제

---

**Q01.** 다음은 Java 코드이다. 올바른 출력 결과를 작성하시오.

```java
public class main {
    public static void main(String[] args) {
        A b = new B();
        b.paint();
        b.draw();
    }
}
class A {
    public void paint() {
        System.out.print("A");
        draw();
    }
    public void draw() {
        System.out.print("B");
        draw();
    }
}
class B extends A {
    public void paint() {
        super.draw();
        System.out.print("C");
        this.draw();
    }
    public void draw() {
        System.out.print("D");
    }
}
```

---

**Q02.** 다음 설명에 알맞은 용어를 보기에서 골라 기호를 작성하시오.

- 사용자가 자격 증명을 공유하지 않고도 타사 애플리케이션이 데이터에 액세스할 수 있도록 권한을 부여하는 개방형 표준 프로토콜이다.
- 액세스 토큰을 사용하여 타사 애플리케이션에 리소스 접근 권한을 부여한다.
- 소셜 미디어 플랫폼에서 널리 사용된다.

[보기] ㄱ.NFT / ㄴ.JWT / ㄷ.SAML / ㄹ.accessToken / ㅁ.SSO / ㅂ.OIDC / ㅅ.Authentication / ㅇ.OAuth / ㅈ.Cookie / ㅊ.Refresh Token

---

**Q03.** 리눅스(Linux)에서 사용자에게 읽기/쓰기/실행 권한을 부여하고, 그룹에게는 읽기/실행 권한을 부여하고, 그 이외에는 실행 권한만 test.txt 파일에 부여하기 위한 명령어를 8진법으로 작성하시오.

```
( (1) ) ( (2) ) test.txt
```

---

**Q04.** 다음은 C언어 코드이다. 올바른 출력 결과를 작성하시오.

```c
#include <stdio.h>
int test(int n) {
    int i, sum = 0;
    for (i = 1; i <= n / 2; i++) {
        if (n % i == 0)
            sum += i;
    }
    if (n == sum)
        return 1;
    return 0;
}
int main() {
    int i, sum = 0;
    for (i = 2; i <= 100; i++) {
        if (test(i))
            sum += i;
    }
    printf("%d ", sum);
    return 0;
}
```

---

**Q05.** 다음은 C언어 코드이다. 출력 결과가 10, 10이 되도록 빈칸에 알맞은 기호를 작성하시오.

```c
#include <stdio.h>
#include <stdlib.h>
typedef struct Data {
    char c;
    int *numPtr;
} Data;
int main() {
    int num = 10;
    Data d1;
    Data *d2 = malloc(sizeof(struct Data));
    d1.numPtr = &num;
    d2 ( ) numPtr = &num;
    printf("%d\n", *d1.numPtr);
    printf("%d\n", *d2 ( ) numPtr);
    free(d2);
    return 0;
}
```

---

**Q06.** 다음 SQL 실행 결과를 작성하시오.

T1

| A |
|---|
| 3 |
| 1 |
| 4 |

T2

| A |
|---|
| 2 |
| 4 |
| 3 |

```sql
SELECT A FROM T1
UNION
SELECT A FROM T2
ORDER BY A DESC;
```

---

**Q07.** 다음 설명에 해당하는 서버 접근 통제 유형을 영어 약자로 작성하시오.

(1) 규칙 기반(Rule-Based) 접근통제 정책으로, 객체에 포함된 정보의 허용 등급과 접근 정보에 대하여 주체가 갖는 접근 허가 권한에 근거하여 객체에 대한 접근을 제한하는 방법이다.

(2) 중앙 관리자가 사용자와 시스템의 상호관계를 통제하며 조직 내 맡은 역할(Role)에 기초하여 자원에 대한 접근을 제한하는 방법이다.

(3) 신분 기반(Identity-Based) 접근통제 정책으로, 주체나 그룹의 신분에 근거하여 객체에 대한 접근을 제한하는 방법이다.

---

**Q08.** 다음은 C언어 코드이다. 올바른 출력 결과를 작성하시오.

```c
#include <stdio.h>
int f(int n) {
    if (n <= 1) return 1;
    else return n * f(n - 1);
}
int main() {
    printf("%d", f(7));
}
```

---

**Q09.** 다음에서 설명하는 용어를 영어 약자로 작성하시오.

자료를 일정한 크기로 정하여 순서대로 전송하는 자료의 전송 방식이다. 셀이라 부르는 고정 길이 패킷(53바이트)을 이용하여 처리가 단순하고 고속망에 적합하며, 가상 채널(Virtual Channel) 기반의 연결 지향 서비스로 통계적 다중화 방식을 이용한다.

---

**Q10.** 다음은 C언어 코드이다. 올바른 출력 결과를 작성하시오.

```c
#include <stdio.h>
int main() {
    char* p = "KOREA";
    printf("%s\n", p);
    printf("%s\n", p + 1);
    printf("%c\n", *p);
    printf("%c\n", *(p + 3));
    printf("%c\n", *p + 4);
}
```

---

**Q11.** 다음은 Java 코드이다. 올바른 출력 결과를 작성하시오.

```java
class Parent {
    int compute(int num) {
        if (num <= 1) return num;
        return compute(num - 1) + compute(num - 2);
    }
}
class Child extends Parent {
    int compute(int num) {
        if (num <= 1) return num;
        return compute(num - 1) + compute(num - 3);
    }
}
public class main {
    public static void main(String args[]) {
        Parent obj = new Child();
        System.out.print(obj.compute(7));
    }
}
```

---

**Q12.** IP 패킷에서 외부의 공인 IP주소와 포트 주소에 해당하는 내부 IP주소를 재기록하여 라우터를 통해 네트워크 트래픽을 주고받는 기술을 무엇이라고 하는가?

---

**Q13.** 다음 Java 코드를 실행할 경우 에러가 발생한다. 에러가 발생하는 라인 번호를 작성하시오.

```java
1  class Person {
2      private String name;
3      public Person(String val) {
4          name = val;
5      }
6      public static String get() {
7          return name;
8      }
9      public void print() {
10         System.out.println(name);
11     }
12 }
13 public class main {
14     public static void main(String[] args) {
15         Person obj = new Person("Kim");
16         obj.print();
17     }
18 }
```

---

**Q14.** 다음은 파이썬 코드이다. 밑줄 친 부분에 들어갈 알맞은 메서드를 작성하시오.

```python
print("파이썬 입출력에 대한 문제입니다.")
num1, num2 = input()._____()
num1 = int(num1)
num2 = int(num2)
print(num1, num2)
num3 = num1 + num2
print(num3)
```

입력값: `2 3`

---

**Q15.** 다음은 판매와 관련된 다이어그램이다. 해당 다이어그램의 명칭을 쓰시오.

*(판매 엔티티와 고객, 상품 엔티티 간의 관계를 표현한 다이어그램)*

---

**Q16.** 다음 설명에 알맞은 테스트 기법을 보기에서 골라 작성하시오.

- 시스템이나 구성요소의 입력값과 출력값이 특정 그룹으로 분류되며, 분류된 그룹 내의 값들이 동일하게 취급된다는 특성을 이용하는 기법
- 하나의 값이 그 분할 내 모든 값을 대표하는 것으로 간주
- 모든 입력값을 시험할 필요가 없게 하여 효율적 테스트 지원
- 일반적으로 경계값 분석 기법과 함께 사용

[보기] ㄱ.Equivalence Partitioning / ㄴ.Statement Coverage / ㄷ.Boundary Value Analysis / ㄹ.Branch Coverage / ㅁ.Error Guessing / ㅂ.Condition Coverage / ㅅ.Cause Effect Graph

---

**Q17.** 다음 설명에 알맞은 클라우드 서비스 유형을 보기에서 골라 작성하시오.

(1) 인터넷을 통해 가상화된 컴퓨팅 자원(서버, 스토리지, 네트워크)을 제공하는 서비스

(2) 소프트웨어 개발, 테스트 환경을 포함한 플랫폼을 제공하는 서비스

(3) 인터넷을 통해 소프트웨어 애플리케이션을 서비스 형태로 제공하는 서비스

[보기] IaaS / PaaS / SaaS

---

**Q18.** 다음에서 설명하는 라우팅 프로토콜을 작성하시오.

- 최소 Hop count를 파악하여 라우팅하는 프로토콜이다.
- 거리와 방향으로 길을 찾아가는 Distance Vector 다이나믹 프로토콜이다.
- 최대 Hop count는 15로, IGP(내부 게이트웨이 프로토콜)에서 많이 사용한다.

---

**Q19.** 다음은 관계 대수에 대한 내용이다. 보기에서 알맞은 기호를 작성하시오.

1. join : ( 1 )
2. project : ( 2 )
3. select : ( 3 )
4. division : ( 4 )

[보기] ㄱ.σ / ㄴ.π / ㄷ.⋈ / ㄹ.÷

---

**Q20.** 다음에서 설명하는 데이터베이스 무결성의 종류를 작성하시오.

관계 데이터베이스에서 2개의 관련 있는 테이블 간의 일관성을 말한다. 외래 키가 선언된 테이블의 외래 키 값은 부모 테이블의 기본 키 값 또는 후보 키 값으로 존재해야 한다.

---
---

## 정답 모음

| 번호 | 정답 | 풀이 |
|------|------|------|
| Q01 | BDCDD | b=new B(). b.paint()→B.paint(): super.draw()=A.draw()→"B"+draw()=B.draw()→"D", print"C", this.draw()→"D". b.draw()→"D". 총 BDCDD |
| Q02 | ㅇ (OAuth) | OAuth: 타사 앱에 사용자 자격 증명 공유 없이 리소스 접근 권한 부여. 액세스 토큰 사용. SNS 로그인에 활용 |
| Q03 | (1) chmod / (2) 751 | 사용자 rwx=7, 그룹 r-x=5, 기타 --x=1 → 751. chmod 명령어로 파일 권한 변경 |
| Q04 | 34 | test()는 완전수 판별 함수. 2~100 완전수: 6(=1+2+3), 28(=1+2+4+7+14). sum=6+28=34 |
| Q05 | -> | 일반 구조체 변수는 `.` 연산자, 포인터 구조체 변수는 `->` 연산자로 멤버 접근. d2->numPtr |
| Q06 | 4 / 3 / 2 / 1 | UNION은 중복 제거: {1,2,3,4}. ORDER BY A DESC → 4,3,2,1 순서 출력 |
| Q07 | (1) MAC / (2) RBAC / (3) DAC | MAC=강제 접근 제어(규칙 기반), RBAC=역할 기반 접근 제어, DAC=임의 접근 제어(신분 기반) |
| Q08 | 5040 | f(7)=7!=7×6×5×4×3×2×1=5040. 재귀 팩토리얼 함수 |
| Q09 | ATM (Asynchronous Transfer Mode) | 비동기 전송 모드. 53바이트 고정 길이 셀. 가상 채널 기반. 고속 네트워크용 |
| Q10 | KOREA / OREA / K / E / O | p+1→"OREA", *p='K', *(p+3)='E'(A=0,O=1,R=2,E=3), *p+4='K'(75)+4=79='O' |
| Q11 | 2 | new Child()로 다형성 적용. Child.compute(7): 재귀 계산 결과=2. compute(-1)=-1, compute(2)=1+(-1)=0, compute(3)=0+0=0, compute(4)=0+1=1, compute(5)=1+0=1, compute(6)=1+0=1, compute(7)=1+1=2 |
| Q12 | NAT (Network Address Translation) | 내부 사설 IP ↔ 외부 공인 IP+포트 변환. 라우터에서 처리. IPv4 주소 절약 및 내부 네트워크 은닉 |
| Q13 | 7 | static 메서드(get())에서 non-static 인스턴스 변수(name) 참조 불가. 7번째 라인 `return name;`에서 컴파일 에러 |
| Q14 | split | input().split()으로 공백 기준 입력 분리. 입력 "2 3" → ["2","3"] 반환 |
| Q15 | ERD (개체-관계 다이어그램) | 엔티티(개체)와 그들 간의 관계를 표현하는 데이터 모델링 다이어그램. 판매 시스템의 데이터 구조 설계에 사용 |
| Q16 | ㄱ (Equivalence Partitioning, 동치 분할) | 입력 도메인을 동등 클래스로 분할하여 각 클래스의 대표값만 테스트. 경계값 분석과 함께 사용 |
| Q17 | (1) IaaS / (2) PaaS / (3) SaaS | IaaS=인프라(서버·스토리지) 제공, PaaS=개발 플랫폼 제공, SaaS=완성된 소프트웨어 서비스 제공 |
| Q18 | RIP (Routing Information Protocol) | 최단 Hop count 기반 Distance Vector 라우팅 프로토콜. 최대 15홉. 소규모 네트워크 IGP 사용 |
| Q19 | (1) ⋈ (ㄷ) / (2) π (ㄴ) / (3) σ (ㄱ) / (4) ÷ (ㄹ) | 관계 대수 기호: σ=셀렉트(행 선택), π=프로젝트(열 선택), ⋈=조인(테이블 결합), ÷=디비전 |
| Q20 | 참조 무결성 (Referential Integrity) | 외래 키 제약: 자식 테이블의 외래 키 값은 부모 테이블의 기본 키/후보 키로 존재해야 함. ON DELETE CASCADE 등으로 관리 |
