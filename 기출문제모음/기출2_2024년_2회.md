# 2024년 정보처리기사 실기 기출문제 — 2회

> 총 20문제

---

**Q01.** 다음 Java 코드를 확인하여 출력값을 작성하시오.

```java
class Main {
    public static void main(String[] args) {
        int[] a = new int[]{1, 2, 3, 4};
        int[] b = new int[]{1, 2, 3, 4};
        int[] c = new int[]{1, 2, 3};

        check(a, b);
        check(a, c);
        check(b, c);
    }

    public static void check(int[] a, int[] b) {
        if (a == b) {
            System.out.print("O");
        } else {
            System.out.print("N");
        }
    }
}
```

---

**Q02.** 다음 설명에 해당하는 용어를 작성하시오.

> 데이터를 중복시켜 성능을 향상시키기 위한 기법이다.
> 데이터를 중복 저장하거나 테이블을 합치는 등의 방법으로 성능을 향상시키지만, 데이터 무결성이 저하될 수 있다.

---

**Q03.** 다음 테이블을 참고하여 SQL문의 빈칸에 들어갈 키워드를 작성하시오.

```text
사원 [사원번호(PK), 이름, 나이, 부서]
부서 [사원번호(PK), 이름, 주소, 나이]
```

```sql
-- ① 신입 사원을 사원 테이블에 추가
INSERT INTO 사원 (사원번호, 이름, 주소, 부서)
[ ① ] (32431, '정실기', '서울', '영업');

-- ② 신입 사원을 검색하여 부서 테이블에 추가
INSERT INTO 부서 (사원번호, 이름, 나이, 부서)
[ ② ] 사원번호, 이름, 나이, 23
FROM 사원
WHERE 이름 = '정실기';

-- ③ 전체 사원 테이블 조회
SELECT *
[ ③ ] 사원;

-- ④ 퇴사자의 부서 값을 '퇴사'로 변경
UPDATE 사원
[ ④ ] 부서 = '퇴사'
WHERE 사원번호 = 32431;
```

---

**Q04.** 다음 릴레이션의 Cardinality와 Degree를 작성하시오.

| 학번 | 이름    | 학과    | 학년 | 성별 |
|------|---------|---------|------|------|
| 001  | 홍길동   | 컴퓨터  | 1   | 남   |
| 002  | 임꺽정   | 전자    | 2   | 남   |
| 003  | 유관순   | 컴퓨터  | 3   | 여   |
| 004  | 신사임당  | 전기   | 4   | 여   |
| 005  | 이순신   | 기계    | 1   | 남   |

```text
Cardinality : ①
Degree      : ②
```

---

**Q05.** 다음 설명에 해당하는 프로토콜을 작성하시오.

- 네트워크 계층에서 IP 패킷을 암호화하고 인증하는 보안 표준이다.
- 기업에서 사설 인터넷망으로 사용할 수 있는 VPN을 구현하는 데 사용된다.
- AH(Authentication Header)와 ESP(Encapsulating Security Payload)라는 두 가지 보안 프로토콜을 사용한다.

---

**Q06.** 다음 Python 코드를 읽고 출력값을 작성하시오.

```python
def fnCalculation(x, y):
    result = 0

    for i in range(len(x)):
        temp = x[i:i + len(y)]

        if temp == y:
            result += 1

    return result


a = "abdcabcabca"
p1 = "ab"
p2 = "ca"

out = f"ab{fnCalculation(a, p1)}ca{fnCalculation(a, p2)}"
print(out)
```

---

**Q07.** 다음 설명에 해당하는 알고리즘을 작성하시오.

- 대칭키 암호 알고리즘이다.
- 1997년 NIST가 DES를 대체하기 위해 공모를 진행했다.
- 128비트, 192비트 또는 256비트 키를 사용한다.
- 블록 크기는 128비트로 고정되어 있다.
- 안전성, 효율성, 속도가 높아 전 세계적으로 널리 사용된다.

---

**Q08.** 패킷 교환 방식 중 다음에 해당하는 방식을 각각 작성하시오.

```text
① 연결형 교환 방식:
② 비연결형 교환 방식:
```

---

**Q09.** 다음 설명에 해당하는 응집도를 보기에서 고르시오.

- 실행 순서가 밀접한 관계를 갖는 기능을 모아 모듈로 구성한다.
- 한 기능 요소의 출력 자료가 다음 기능 요소의 입력 자료로 사용된다.

[보기]

```text
ㄱ. 기능적 응집도(Functional Cohesion)
ㄴ. 우연적 응집도(Coincidental Cohesion)
ㄷ. 교환적 응집도(Communicational Cohesion)
ㄹ. 절차적 응집도(Procedural Cohesion)
ㅁ. 시간적 응집도(Temporal Cohesion)
ㅂ. 순차적 응집도(Sequential Cohesion)
ㅅ. 논리적 응집도(Logical Cohesion)
```

---

**Q10.** 다음 설명에 해당하는 디자인 패턴을 보기에서 고르시오.

- 컬렉션 객체의 내부 구조를 노출하지 않고 요소에 순차적으로 접근할 수 있게 한다.
- 객체의 내부 표현 방식과 관계없이 요소에 접근할 수 있다.
- 반복 과정을 캡슐화하여 클라이언트가 컬렉션의 구체적인 구현에 종속되지 않도록 한다.

[보기]

| 생성 패턴 | 구조 패턴 | 행위 패턴 |
|-----------|-----------|-----------|
| Singleton | Adapter | Iterator |
| Factory Method | Bridge | Visitor |
| Abstract Factory | Composite | Observer |

---

**Q11.** 다음 그림을 바탕으로 RIP 방식으로 최단 경로 비용을 계산하고, 흐름에 맞게 작성하시오.

*(라우터 네트워크 토폴로지: A→B 비용2, A→C 비용8, B→D 비용3, C→D 비용1)*

```text
A →
```

---

**Q12.** 다음 표를 참고하여 SRT(Shortest Remaining Time) 스케줄링의 평균 대기 시간을 계산하시오.

| 프로세스 | 도착 시간 | 서비스 시간 |
|---------|----------|-----------|
| A | 0 | 8 |
| B | 1 | 4 |
| C | 2 | 9 |
| D | 3 | 5 |

---

**Q13.** 다음 C 코드를 확인하여 출력값을 작성하시오.

```c
#include <stdio.h>

int main() {
    int arr[3][3] = {
        1, 2, 3,
        4, 5, 6,
        7, 8, 9
    };

    int* parr[2] = {arr[1], arr[2]};

    printf("%d", parr[1][1] + *(parr[1] + 2) + **parr);

    return 0;
}
```

---

**Q14.** 다음 Java 코드를 확인하여 출력값을 작성하시오.

```java
class Main {
    public static void main(String[] args) {
        int a[] = {1, 2, 3, 4, 5, 6, 7, 8, 9};

        ODDNumber OE = new ODDNumber();

        System.out.print(
            OE.sum(a, true) + ", " + OE.sum(a, false)
        );
    }
}

interface Number {
    int sum(int[] a, boolean odd);
}

class ODDNumber implements Number {
    public int sum(int[] a, boolean odd) {
        int result = 0;

        for (int i = 0; i < a.length; i++) {
            if ((odd && a[i] % 2 != 0) ||
                (!odd && a[i] % 2 == 0)) {
                result += a[i];
            }
        }

        return result;
    }
}
```

---

**Q15.** 다음 C 코드를 확인하여 출력값을 작성하시오.

```c
#include <stdio.h>
#include <string.h>

void sumFn(char* d, const char* s) {
    while (*s) {
        *d = *s;
        d++;
        s++;
    }

    *d = '\0';
}

int main() {
    const char* str1 = "first";
    char str2[50] = "teststring";
    int result = 0;

    sumFn(str2, str1);

    for (int i = 0; str2[i] != '\0'; i++) {
        result += i;
    }

    printf("%d", result);

    return 0;
}
```

---

**Q16.** 다음 설명에 해당하는 결합도를 작성하시오.

- 한 모듈이 다른 모듈 내부의 논리적 흐름을 제어하기 위해 제어 요소를 전달하는 결합도이다.
- 한 모듈이 다른 모듈의 상세 처리 절차를 알고 이를 통제하는 경우에 발생한다.
- 처리 기능이 두 모듈에 분리되어 설계된 경우에도 발생할 수 있다.

```text
(              ) Coupling
```

---

**Q17.** 다음 Java 코드를 확인하여 출력값을 작성하시오.

```java
class Main {
    public static void main(String[] args) {
        String str = "abacabcd";
        boolean[] seen = new boolean[256];

        System.out.print(
            calculFn(str, str.length() - 1, seen)
        );
    }

    public static String calculFn(
        String str,
        int index,
        boolean[] seen
    ) {
        if (index < 0) {
            return "";
        }

        char c = str.charAt(index);
        String result = calculFn(str, index - 1, seen);

        if (!seen[c]) {
            seen[c] = true;
            return c + result;
        }

        return result;
    }
}
```

---

**Q18.** 다음 C 코드를 확인하여 출력값을 작성하시오.

```c
#include <stdio.h>

void swap(int a, int b) {
    int t = a;
    a = b;
    b = t;
}

int main() {
    int a = 11;
    int b = 19;

    swap(a, b);

    switch (a) {
        case 1:
            b += 1;

        case 11:
            b += 2;

        default:
            b += 3;
            break;
    }

    printf("%d", a - b);
}
```

---

**Q19.** 다음 C 코드를 확인하여 출력값을 작성하시오.

```c
#include <stdio.h>

struct node {
    int n1;
    struct node* n2;
};

int main() {
    struct node a = {10, NULL};
    struct node b = {20, NULL};
    struct node c = {30, NULL};

    struct node* head = &a;

    a.n2 = &b;
    b.n2 = &c;

    printf("%d\n", head->n2->n1);

    return 0;
}
```

---

**Q20.** 다음 Java 코드를 확인하여 출력값을 작성하시오.

```java
class Main {
    public static void main(String[] args) {
        String str = "ITISTESTSTRING";
        String[] result = str.split("T");

        System.out.print(result[3]);
    }
}
```

---

## 정답 모음

| 번호 | 정답 |
|------|------|
| Q01 | NNN |
| Q02 | 반정규화(Denormalization) |
| Q03 | ①VALUES, ②SELECT, ③FROM, ④SET |
| Q04 | ①Cardinality: 5, ②Degree: 5 |
| Q05 | IPSec |
| Q06 | ab3ca3 |
| Q07 | AES |
| Q08 | ①가상 회선(Virtual Circuit), ②데이터그램(Datagram) |
| Q09 | ㅂ. 순차적 응집도(Sequential Cohesion) |
| Q10 | Iterator |
| Q11 | A→B→D, 비용: 5 |
| Q12 | 6.5ms |
| Q13 | 21 |
| Q14 | 25, 20 |
| Q15 | 10 |
| Q16 | Control(제어) Coupling |
| Q17 | dcba |
| Q18 | -13 |
| Q19 | 20 |
| Q20 | S |
