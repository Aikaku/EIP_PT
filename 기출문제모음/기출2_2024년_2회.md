# 2024년 정보처리기사 실기 기출문제 — 2회

> 총 20문제

---

**Q01.** 다음 Java 코드에서 알맞은 출력값을 작성하시오.

```java
class Main {
    public static void main(String[] args) {
        int[] a = new int[]{1, 2, 3, 4};
        int[] b = new int[]{1, 2, 3, 4};
        int[] c = new int[]{1, 2, 3};
        check(a, b);
        check(a, c);
        check(b, c);
    }
    public static void check(int[] a, int[] b) {
        if (a == b) {
            System.out.print("O");
        } else {
            System.out.print("N");
        }
    }
}
```

---

**Q02.** 다음에서 설명하는 용어를 작성하시오.

데이터를 중복시켜 성능을 향상시키기 위한 기법으로, 데이터를 중복 저장하거나 테이블을 합치는 등으로 성능을 향상시키지만 데이터 무결성이 저하될 수 있는 기법

---

**Q03.** 다음 SQL 구문의 빈칸을 작성하시오.

```sql
-- 신입 사원이 들어와서 사원 테이블에 추가
INSERT INTO 사원 (사원번호, 이름, 주소, 부서) [ ① ] (32431, '정실기', '서울', '영업');

-- 위에 신입사원을 검색하면서 부서 테이블에 추가
INSERT INTO 부서 (사원번호, 이름, 나이, 부서)
[ ② ] 사원번호, 이름, 나이, 23 FROM 사원 WHERE 이름 = '정실기';

-- 전체 사원 테이블 조회
SELECT * [ ③ ] 사원;

-- 퇴사로 인해 부서에 해당하는 값을 '퇴사'로 변경
UPDATE 사원 [ ④ ] 부서 = '퇴사' WHERE 사원번호 = 32431;
```

---

**Q04.** 다음 릴레이션의 Cardinality와 Degree를 작성하시오.

| 학번 | 이름 | 학과 | 학년 | 성별 |
|------|------|------|------|------|
| 001  | 홍길동 | 컴퓨터 | 1 | 남 |
| 002  | 임꺽정 | 전자   | 2 | 남 |
| 003  | 유관순 | 컴퓨터 | 3 | 여 |
| 004  | 신사임당 | 전기 | 4 | 여 |
| 005  | 이순신 | 기계   | 1 | 남 |

Cardinality: ( ① )
Degree: ( ② )

---

**Q05.** 다음 내용을 읽고 알맞은 답을 작성하시오.

- Network layer에서 IP 패킷을 암호화하고 인증하는 등의 보안을 위한 표준이다.
- 기업에서 사설 인터넷망으로 사용할 수 있는 VPN을 구현하는데 사용되는 프로토콜이다.
- AH(Authentication Header)와 ESP(Encapsulating Security Payload)라는 두 가지 보안 프로토콜을 사용한다.

---

**Q06.** 다음 Python 코드에 대한 알맞은 출력 값을 작성하시오.

```python
def fnCalculation(x, y):
    result = 0
    for i in range(len(x)):
        temp = x[i:i+len(y)]
        if temp == y:
            result += 1
    return result

a = "abdcabcabca"
p1 = "ab"
p2 = "ca"
out = f"ab{fnCalculation(a, p1)}ca{fnCalculation(a, p2)}"
print(out)
```

---

**Q07.** 다음 내용을 읽고 알맞은 알고리즘을 작성하시오.

- 대칭키 알고리즘으로 1997년 NIST에서 DES를 대체하기 위해 생성되었다.
- 128비트, 192비트 또는 256비트의 가변 키 크기와 128비트의 고정 블록 크기를 사용한다.
- 높은 안전성과 효율성으로 DES 대신 전 세계적으로 많이 사용되고 있다.

---

**Q08.** 패킷 교환 방식 중에서 연결형과 비연결형에 해당하는 방식을 작성하시오.

① 연결형 교환 방식:
② 비연결형 교환 방식:

---

**Q09.** 다음 설명에서 각각 해당하는 응집도를 보기에서 골라 작성하시오.

(1) 실행 순서가 밀접한 관계를 갖는 기능을 모아 모듈로 구성한다.
(2) 한 모듈 내부의 한 기능 요소에 의한 출력 자료가 다음 기능 원소의 입력 자료로서 제공되는 형태이다.

[보기] ㄱ.기능적(functional) / ㄴ.우연적(Coincidental) / ㄷ.통신적(Communication) / ㄹ.절차적(Procedural) / ㅁ.시간적(Temporal) / ㅂ.순차적(sequential) / ㅅ.논리적(Logical)

---

**Q10.** 다음 디자인 패턴에 관한 설명에서 알맞은 용어를 작성하시오.

- 컬렉션 객체의 내부 구조를 노출하지 않고 순차적으로 접근할 수 있게 하는 패턴이다.
- 객체의 내부 표현 방식에 독립적으로 요소에 접근할 수 있도록 해준다.
- 반복 프로세스를 캡슐화하여 클라이언트 코드에서 컬렉션의 구체적 구현에 종속되지 않도록 한다.

---

**Q11.** 다음 그림을 바탕으로 RIP을 구성하여 최단 경로 비용을 계산하시오.

*(라우터 네트워크 토폴로지 다이어그램: A→B 비용2, A→C 비용8, B→D 비용3, C→D 비용1 구조)*

---

**Q12.** 다음 SRT 스케줄링의 평균 대기시간을 계산하여 작성하시오.

| 프로세스 | 도착 시간 | 서비스 시간 |
|---------|----------|-----------|
| A | 0 | 8 |
| B | 1 | 4 |
| C | 2 | 9 |
| D | 3 | 5 |

---

**Q13.** 다음 C언어 코드에서 알맞은 출력값을 작성하시오.

```c
#include <stdio.h>
int main() {
    int arr[3][3] = {1, 2, 3, 4, 5, 6, 7, 8, 9};
    int* parr[2] = {arr[1], arr[2]};
    printf("%d", parr[1][1] + *(parr[1] + 2) + **parr);
    return 0;
}
```

---

**Q14.** 다음 Java 코드에서 알맞은 출력값을 작성하시오.

```java
class Main {
    public static void main(String[] args) {
        int[] a = {1, 2, 3, 4, 5, 6, 7, 8, 9};
        ODDNumber OE = new ODDNumber();
        System.out.print(OE.sum(a, true) + ", " + OE.sum(a, false));
    }
}
interface Number {
    int sum(int[] a, boolean odd);
}
class ODDNumber implements Number {
    public int sum(int[] a, boolean odd) {
        int result = 0;
        for (int i = 0; i < a.length; i++) {
            if ((odd && a[i] % 2 != 0) || (!odd && a[i] % 2 == 0))
                result += a[i];
        }
        return result;
    }
}
```

---

**Q15.** 다음 C언어 코드에서 알맞은 출력값을 작성하시오.

```c
#include <stdio.h>
#include <string.h>
void sumFn(char* d, const char* s) {
    while (*s) {
        *d = *s;
        d++;
        s++;
    }
    *d = '\0';
}
int main() {
    const char* str1 = "first";
    char str2[50] = "teststring";
    int result = 0;
    sumFn(str2, str1);
    for (int i = 0; str2[i] != '\0'; i++) {
        result += i;
    }
    printf("%d", result);
    return 0;
}
```

---

**Q16.** 다음 설명에서 괄호 안에 알맞은 결합도를 작성하시오.

어떤 모듈이 다른 모듈 내부의 논리적인 흐름을 제어하기 위해, 제어를 통신하거나 제어 요소를 전달하는 결합도이다. 한 모듈이 다른 모듈의 상세한 처리 절차를 알고 있어 이를 통제하는 경우나 처리 기능이 두 모듈에 분리되어 설계된 경우에 발생한다.

( ) Coupling

---

**Q17.** 다음 Java 코드에서 알맞은 출력 값을 작성하시오.

```java
class Main {
    public static void main(String[] args) {
        String str = "abacabcd";
        boolean[] seen = new boolean[256];
        System.out.print(calculFn(str, str.length() - 1, seen));
    }
    public static String calculFn(String str, int index, boolean[] seen) {
        if (index < 0) return "";
        char c = str.charAt(index);
        String result = calculFn(str, index - 1, seen);
        if (!seen[c]) {
            seen[c] = true;
            return c + result;
        }
        return result;
    }
}
```

---

**Q18.** 다음 C언어 코드에서 알맞은 출력 값을 작성하시오.

```c
#include <stdio.h>
void swap(int a, int b) {
    int t = a;
    a = b;
    b = t;
}
int main() {
    int a = 11;
    int b = 19;
    swap(a, b);
    switch (a) {
        case 1:  b += 1;
        case 11: b += 2;
        default: b += 3;
            break;
    }
    printf("%d", a - b);
}
```

---

**Q19.** 다음 C언어 구조체 코드에서 알맞은 출력 값을 작성하시오.

```c
#include <stdio.h>
struct node {
    int n1;
    struct node* n2;
};
int main() {
    struct node a = {10, NULL};
    struct node b = {20, NULL};
    struct node c = {30, NULL};
    struct node* head = &a;
    a.n2 = &b;
    b.n2 = &c;
    printf("%d\n", head->n2->n1);
    return 0;
}
```

---

**Q20.** 다음 Java 코드에서 알맞은 출력 값을 작성하시오.

```java
class Main {
    public static void main(String[] args) {
        String str = "ITISTESTSTRING";
        String[] result = str.split("T");
        System.out.print(result[3]);
    }
}
```

---
---

## 정답 모음

| 번호 | 정답 | 풀이 |
|------|------|------|
| Q01 | NNN | 배열은 참조 타입. a==b는 주소 비교. a,b,c 모두 다른 객체 → 모두 false → N,N,N |
| Q02 | 반정규화 (Denormalization) | 정규화된 테이블을 성능 목적으로 의도적 중복 허용. 조회 성능↑, 무결성↓ |
| Q03 | ①VALUES / ②SELECT / ③FROM / ④SET | DML 구문: INSERT~VALUES, INSERT~SELECT, SELECT~FROM, UPDATE~SET |
| Q04 | ①Cardinality: 5 / ②Degree: 5 | Cardinality=튜플(행) 수=5, Degree=속성(열) 수=5(학번,이름,학과,학년,성별) |
| Q05 | IPSec (IP Security) | OSI 3계층(네트워크) 보안 프로토콜. AH(무결성)+ESP(암호화). VPN 구현에 사용 |
| Q06 | ab3ca3 | fnCalculation("abdcabcabca","ab")=3 (인덱스 0,4,7), fnCalculation(a,"ca")=3 (인덱스 3,6,9). f-string 결합: "ab3ca3" |
| Q07 | AES (Advanced Encryption Standard) | 2001년 NIST 제정. DES 대체. 블록 128비트 고정, 키 128/192/256비트 선택 |
| Q08 | ①가상 회선 (Virtual Circuit) / ②데이터그램 (Datagram) | 가상회선=연결 설정 후 고정 경로, 데이터그램=패킷마다 독립 경로 선택 |
| Q09 | (1) ㄹ 절차적(Procedural) / (2) ㅂ 순차적(sequential) | 절차적=실행 순서 중심, 순차적=이전 출력이 다음 입력으로 연결 |
| Q10 | Iterator (이터레이터) 패턴 | 행위 패턴. 컬렉션 내부 노출 없이 순차 접근. for-each의 기반 개념 |
| Q11 | A→B→D: 5 | RIP은 최소 Hop count 기반. A→B=2, B→D=3 → 비용=5 |
| Q12 | 6.5 | SRT: B완료=5(대기0), D완료=10(대기2), A완료=17(대기9), C완료=26(대기15). 평균=(0+2+9+15)/4=26/4=6.5 |
| Q13 | 21 | arr[1]={4,5,6}, arr[2]={7,8,9}. parr[0]=arr[1], parr[1]=arr[2]. parr[1][1]=8, *(parr[1]+2)=9, **parr=4 → 8+9+4=21 |
| Q14 | 25, 20 | sum(a,true)=홀수합=1+3+5+7+9=25, sum(a,false)=짝수합=2+4+6+8=20 |
| Q15 | 10 | sumFn: str2="first"(5글자). for i=0~4: result=0+1+2+3+4=10 |
| Q16 | Control (제어 결합도) | 제어 결합도: 플래그나 제어 신호를 통해 다른 모듈의 흐름 제어. 내용>공통>외부>제어>스탬프>자료 순서 |
| Q17 | dcba | 재귀로 index 0부터 처음 등장 문자만 선택. 'a'→"a", 'b'→"ba", 'c'→"cba", 'd'→"dcba" |
| Q18 | -13 | swap은 값 복사(무효). a=11. switch case11: b+=2=21, default: b+=3=24. a-b=11-24=-13 |
| Q19 | 20 | head=&a, head->n2=a.n2=&b, head->n2->n1=b.n1=20 |
| Q20 | S | "ITISTESTSTRING".split("T") → ["I","IS","ES","S","RING"]. result[3]="S" |
