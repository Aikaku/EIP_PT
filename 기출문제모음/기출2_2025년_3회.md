# 2025년 정보처리기사 실기 기출문제 — 3회

> 총 20문제

---

**Q01.** 다음 C언어 코드에서 알맞은 출력 값을 작성하시오.

```c
#include <stdio.h>
struct Test {
    int i;
    const char *g;
};
int main() {
    struct Test test[] = {{1, "AB"}, {2, "DC"}, {3, "EB"}};
    struct Test *p = &test[1];
    printf("%s", p->g + (p->i - 1));
    return 0;
}
```

---

**Q02.** 다음 C언어 코드에서 알맞은 출력 값을 작성하시오.

```c
#include <stdio.h>
int main(void) {
    char str[] = "REPUBLICOFKOREA";
    int a = 0;
    while (str[a] != '\0')
        ++a;
    putchar(str[a - 2]);
    return 0;
}
```

---

**Q03.** 다음 C언어 코드에서 알맞은 출력 값을 작성하시오.

```c
#include <stdio.h>
struct Node {
    struct Node* next;
    unsigned int x;
};
int main() {
    struct Node t1 = {0, 5u};
    struct Node t2 = {0, 7u};
    struct Node t3 = {0, 11u};
    t3.next = &t2;
    t2.next = &t1;
    struct Node* curr = &t3;
    int sum = 0;
    while (curr) {
        sum = sum * 3 + curr->x;
        curr = curr->next;
    }
    sum = (sum ^ 42u) + 100u;
    printf("%u\n", sum);
}
```

---

**Q04.** 다음 Java 코드에서 빈칸 `____`에 들어갈 알맞은 키워드를 작성하시오.

```java
interface Machine {
    void run();
}
class WashingMachine ____ Machine {
    private String name;
    public WashingMachine() {
        this.name = "LG Washer";
    }
    public void run() {
        System.out.println("Washing machine running");
    }
}
public class Main {
    public static void main(String[] args) {
        WashingMachine wm = new WashingMachine();
        wm.run();
    }
}
```

---

**Q05.** 다음 Python 코드에서 알맞은 출력 값을 작성하시오.

```python
data = [
    [3, 5, 2, 4, 1],
    [4, 5, 1],
    [4, 4, 1, 5, 4],
    [4, 5]
]
result = {}
for index, lis in enumerate(data):
    list_sum = sum(lis)
    list_len = len(lis)
    result[index] = (list_sum, list_len)
print(result)
```

---

**Q06.** 다음 Java 코드에서 빈칸 `____`에 들어갈 알맞은 키워드를 작성하시오.

```java
class Rectangle {
    int width, height;
    Rectangle(int width, int height) {
        this.width = width;
        this.height = height;
    }
}
class Square extends Rectangle {
    Square(int a) {
        ____(a, a);
    }
    int getSquareArea() {
        return width * height;
    }
}
public class Main {
    public static void main(String[] args) {
        Square sq = new Square(10);
        System.out.println(sq.getSquareArea());
    }
}
```

---

**Q07.** 다음 C언어 코드에서 알맞은 출력 값을 작성하시오.

```c
#include <stdio.h>
int main() {
    int x = 7, y = 4, z;
    z = y % 3 < 3 ? 2 : 1;
    z = z & z >> 1;
    z = x > 5 && z <= 3 ? z * x : z / x;
    printf("%d", z);
    return 0;
}
```

---

**Q08.** 다음 Java 코드에서 알맞은 출력 값을 작성하시오.

```java
enum Tri {
    A("A"), B("AB"), C("ABC");
    private String code;
    Tri(String code) {
        this.code = code;
    }
    public String code() {
        return code;
    }
}
public class Main {
    public static void main(String[] args) {
        Tri t = Tri.values()[Tri.A.name().length()];
        System.out.print(t.code());
    }
}
```

---

**Q09.** 다음에서 설명하는 용어를 작성하시오.

- 여러 사용자가 동시에 데이터베이스를 사용할 때 데이터의 일관성을 유지하기 위한 기법이다.
- 특정 데이터에 대한 접근을 한 번에 하나의 트랜잭션만 허용한다.
- 공유(Shared) 잠금과 배타적(Exclusive) 잠금으로 구분된다.

---

**Q10.** 다음에서 설명하는 소프트웨어 테스트 기법을 작성하시오.

입력 값의 범위를 여러 구간으로 나누어 각 구간에서 대표적인 값을 하나씩 선택하여 테스트하는 기법이다. 같은 구간 내의 값은 동일하게 처리된다는 가정을 기반으로 한다.

---

**Q11.** 다음에서 설명하는 네트워크 보안 프로토콜을 작성하시오.

- 웹 브라우저와 서버 간의 통신을 암호화하는 프로토콜이다.
- HTTPS의 기반이 되는 프로토콜이다.
- Transport Layer Security의 약자로 SSL을 대체한다.

---

**Q12.** 다음 UML 다이어그램 관계에서 설명하는 것을 작성하시오.

한 클래스가 다른 클래스를 포함하는 관계로, 전체-부분(Whole-Part) 관계이다. 포함된 클래스는 포함하는 클래스 없이 독립적으로 존재할 수 없다. 다이아몬드가 채워진(●) 형태로 표현한다.

---

**Q13.** 다음에서 설명하는 개념을 작성하시오.

릴레이션에서 한 속성의 값이 다른 속성의 값에 의해 함수적으로 결정되는 관계. A→B에서 A가 결정자, B가 종속자이며, A의 값이 동일하면 B의 값도 항상 동일하다.

---

**Q14.** 다음에서 설명하는 공격 유형을 작성하시오.

- 정상적인 서비스 요청처럼 위장하여 서버의 처리 능력을 소진시키는 공격이다.
- 분산된 다수의 좀비 컴퓨터를 이용하여 공격 대상에 동시에 대량의 패킷을 전송한다.
- Distributed Denial of Service의 약자이다.

---

**Q15.** 다음에서 설명하는 소프트웨어 개발 품질 모델을 작성하시오.

SW 개발 조직의 프로세스 성숙도를 평가하는 모델로, 초기(Initial) → 관리(Managed) → 정의(Defined) → 정량적 관리(Quantitatively Managed) → 최적화(Optimizing)의 5단계로 구성된다.

---

**Q16.** 다음에서 설명하는 응집도(Cohesion) 유형을 작성하시오.

모듈 내의 모든 요소들이 단일한 잘 정의된 기능을 수행할 때 나타나는 응집도이다. 가장 높은 수준의 응집도로 모듈의 독립성이 가장 높다.

---

**Q17.** 다음 SQL에서 빈칸에 들어갈 알맞은 명령어를 작성하시오.

```sql
-- 테이블 생성
CREATE TABLE 직원 (
    직원번호 INT ( ① ) KEY,
    이름 VARCHAR(50) NOT NULL,
    부서코드 INT,
    ( ② ) KEY (부서코드) REFERENCES 부서(부서코드)
);
```

---

**Q18.** 다음에서 설명하는 개념을 작성하시오.

데이터베이스에서 정규화 과정을 거쳐 여러 테이블로 분리된 데이터를 의도적으로 다시 합치거나 중복을 허용하여 조회 성능을 향상시키는 기법이다.

---

**Q19.** 다음에서 설명하는 OS 메모리 관리 기법을 작성하시오.

- 프로그램이 실제 메모리보다 클 경우에도 실행 가능하게 하는 기술이다.
- 보조기억장치(디스크)의 일부를 주기억장치처럼 사용한다.
- 페이징과 세그멘테이션 기법이 있다.

---

**Q20.** 다음에서 설명하는 용어를 작성하시오.

소프트웨어 개발에서 반복적인 작업을 자동화하는 개념으로, 코드 변경이 발생할 때마다 자동으로 빌드·테스트·배포가 수행된다. CI(지속적 통합), CD(지속적 배포)를 포함하는 파이프라인으로 구성된다.

---
---

## 정답 모음

| 번호 | 정답 | 풀이 |
|------|------|------|
| Q01 | C | p=&test[1]→p->i=2, p->g="DC". p->g+(2-1)="DC"+1=포인터가 'C'를 가리킴. printf("%s")→"C" |
| Q02 | E | "REPUBLICOFKOREA"=15글자(인덱스0~14). 루프 후 a=15. str[15-2]=str[13]='E' (R,E,P,U,B,L,I,C,O,F,K,O,R,E,A) |
| Q03 | 187 | curr=t3: sum=0*3+11=11. curr=t2: sum=11*3+7=40. curr=t1: sum=40*3+5=125. (125^42)+100=87+100=187 |
| Q04 | implements | 인터페이스 구현 키워드. class ClassName implements InterfaceName. extends는 클래스 상속에 사용 |
| Q05 | {0: (15, 5), 1: (10, 3), 2: (18, 5), 3: (9, 2)} | 각 리스트의 합과 길이를 tuple로 저장. sum([3,5,2,4,1])=15, len=5. sum([4,5,1])=10, len=3. sum([4,4,1,5,4])=18, len=5. sum([4,5])=9, len=2 |
| Q06 | super | 부모 클래스 생성자 호출 키워드. Square(10)→super(10,10)→Rectangle.width=10, height=10. getSquareArea()=100 |
| Q07 | 0 | z=y%3<3? 4%3=1<3→true→z=2. z=2&(2>>1)=2&1=0b10&0b01=0. z=7>5&&0<=3? true→z=0*7=0 |
| Q08 | AB | Tri.A.name()="A", "A".length()=1. Tri.values()[1]=B. B.code()="AB" |
| Q09 | 잠금 (Locking) | 동시성 제어 기법. 공유 잠금(읽기 가능, 쓰기 불가)·배타 잠금(읽기·쓰기 모두 불가). 교착상태 주의 |
| Q10 | 동등 분할 (Equivalence Partitioning) | 입력 도메인을 동등 클래스로 분할하여 대표값 선택. 경계값 분석과 함께 사용. 명세 기반(블랙박스) 기법 |
| Q11 | TLS (Transport Layer Security) | HTTPS 기반 암호화 프로토콜. SSL 3.0에서 발전. 현재 TLS 1.3이 최신. 인증서·대칭키 암호화 사용 |
| Q12 | 합성(Composition) 관계 | UML 연관 관계. ●로 표시(채운 다이아몬드). 강한 소유 관계. vs. 집합(Aggregation)=빈 다이아몬드(◇) |
| Q13 | 함수 종속 (Functional Dependency) | A→B: A가 B를 함수적으로 결정. 정규화의 기반 개념. 완전/부분/이행 함수 종속으로 구분 |
| Q14 | DDoS (Distributed Denial of Service) | 분산 서비스 거부 공격. 봇넷(좀비PC)을 이용한 대량 요청. DoS(단일 출처)와 구분 |
| Q15 | CMMI (Capability Maturity Model Integration) | 프로세스 성숙도 5단계 모델. 1:초기→2:관리→3:정의→4:정량적관리→5:최적화. SEI 개발 |
| Q16 | 기능적(Functional) 응집도 | 가장 높은 수준의 응집도. 단일 기능만 수행. 응집도 순: 기능>순차>통신>절차>시간>논리>우연 |
| Q17 | ①PRIMARY / ②FOREIGN | PRIMARY KEY: 기본키 지정. FOREIGN KEY: 외래키 지정. REFERENCES로 참조 테이블·컬럼 지정 |
| Q18 | 반정규화 (Denormalization) | 성능 향상 목적의 의도적 중복 허용. 테이블 통합·분할, 중복 컬럼 추가 등. 무결성↓ 성능↑ trade-off |
| Q19 | 가상 메모리 (Virtual Memory) | 보조기억장치를 주기억장치처럼 활용. 스와핑·페이징·세그멘테이션 기법. 프로세스가 실제 물리 메모리보다 크게 사용 가능 |
| Q20 | CI/CD 파이프라인 | CI(Continuous Integration)=자동 빌드·테스트. CD(Continuous Delivery/Deployment)=자동 배포. Jenkins, GitHub Actions 등 도구 |
