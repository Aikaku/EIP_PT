# 2024년 정보처리기사 실기 기출문제 — 1회

> 총 20문제

---

**Q01.** 다음 Java 코드에서 알맞은 출력 값을 작성하시오.

```java
class Connection {
    private static Connection _inst = null;
    private int count = 0;
    static public Connection get() {
        if (_inst == null) {
            _inst = new Connection();
            return _inst;
        }
        return _inst;
    }
    public void count() { count++; }
    public int getCount() { return count; }
}
public class main {
    public static void main(String[] args) {
        Connection conn1 = Connection.get();
        conn1.count();
        Connection conn2 = Connection.get();
        conn2.count();
        Connection conn3 = Connection.get();
        conn3.count();
        conn1.count();
        System.out.print(conn1.getCount());
    }
}
```

---

**Q02.** 다음 C언어 코드에서 알맞은 출력 값을 작성하시오.

```c
#include <stdio.h>
int main() {
    int v1 = 0, v2 = 35, v3 = 29;
    if (v1 > v2 ? v2 : v1) {
        v2 = v2 << 2;
    } else {
        v3 = v3 << 2;
    }
    printf("%d", v2 + v3);
    return 0;
}
```

---

**Q03.** 다음은 응집도와 관련한 문제이다. 보기에서 응집도가 높은 순으로 나열하시오.

보기: ㄱ.기능 / ㄴ.교환 / ㄷ.우연 / ㄹ.시간

---

**Q04.** 다음은 C언어 코드이다. 알맞은 출력 값을 작성하시오.

```c
#include <stdio.h>
#include <string.h>
void reverse(char* str) {
    int len = strlen(str);
    char temp;
    char* p1 = str;
    char* p2 = str + len - 1;
    while (p1 < p2) {
        temp = *p1;
        *p1 = *p2;
        *p2 = temp;
        p1++;
        p2--;
    }
}
int main(int argc, char* argv[]) {
    char str[100] = "ABCDEFGH";
    reverse(str);
    int len = strlen(str);
    for (int i = 1; i < len; i += 2) {
        printf("%c", str[i]);
    }
    printf("\n");
    return 0;
}
```

---

**Q05.** 다음 네트워크에서 라우터를 통한 할당 가능한 2번, 4번, 5번의 IP를 보기에서 골라 작성하시오.

- 1번: 192.168.35.3/24
- 3번: 129.200.10.16/22
- 6번: 192.168.36.24/24

[보기] 192.168.35.0 / 192.168.35.72 / 192.168.36.0 / 192.168.36.249 / 129.200.8.0 / 129.200.8.249

---

**Q06.** 다음 테이블에서 나타나고 있는 정규형을 작성하시오.

| 고객아이디 | 강좌명 | 강사번호 |
|-----------|--------|---------|
| apple | 영어회화 | P001 |
| banana | 기초토익 | P002 |
| carrot | 영어회화 | P001 |
| carrot | 기초토익 | P004 |
| orange | 영어회화 | P003 |
| orange | 기초토익 | P004 |

---

**Q07.** 다음 설명에 해당하는 네트워크 용어를 영문 약자로 작성하시오.

1. 대표적인 링크 상태 라우팅 프로토콜이다. 인터넷에서 연결된 링크의 상태를 감시하여 최적의 경로를 선택한다.
2. 단일 자율 시스템 내에서 라우팅 정보를 배포하는 데 사용되는 내부 게이트웨이 프로토콜이다.
3. 모든 대상에 도달하기 위한 최단 경로를 구축하고, Dijkstra 알고리즘을 사용하여 계산한다.

---

**Q08.** 다음 각 설명에 해당하는 조인의 종류를 작성하시오.

(1) 조인에 참여하는 두 릴레이션의 속성 값을 비교하여 조건을 만족하는 튜플만 반환한다.
(2) 조건이 정확하게 '='(등호)로 일치하는 결과를 반환한다.
(3) (2) 조인에서 조인에 참여한 속성이 두 번 나오지 않도록 중복된 속성을 제거한 결과를 반환한다.

---

**Q09.** 다음 페이지 참조 순서를 참고하여 할당된 프레임 수가 3개일 때 LRU와 LFU 알고리즘의 페이지 부재 횟수를 작성하시오.

페이지 참조 순서: 1, 2, 3, 1, 2, 4, 1, 2, 5, 7

(1) LRU:
(2) LFU:

---

**Q10.** 다음 Java 코드의 실행 순서를 중복 번호 없이 작성하시오.

```java
class Parent {
    int x, y;
    Parent(int x, int y) {   // ①
        this.x = x;
        this.y = y;          // ②
    }
    int getT() {
        return x * y;        // ③
    }
}
class Child extends Parent {
    int x;
    Child(int x) {           // ④
        super(x + 1, x);
        this.x = x;          // ⑤
    }
    int getT(int n) {
        return super.getT() + n;
    }
}
class Main {
    public static void main(String[] args) { // ⑥
        Parent parent = new Child(3);        // ⑦
        System.out.println(parent.getT());
    }
}
```

실행 순서: 6 → ( ) → ( ) → ( ) → ( ) → ( )

---

**Q11.** 다음 C언어 코드의 알맞은 출력 값을 작성하시오.

```c
#include <stdio.h>
typedef struct {
    int accNum;
    double bal;
} BankAcc;

double sim_pow(double base, int year) {
    int i;
    double r = 1.0;
    for (i = 0; i < year; i++) {
        r = r * base;
    }
    return r;
}
void initAcc(BankAcc* acc, int x, double y) {
    acc->accNum = x;
    acc->bal = y;
}
void xxx(BankAcc* acc, double* en) {
    if (*en > 0 && *en < acc->bal) {
        acc->bal = acc->bal - *en;
    } else {
        acc->bal = acc->bal + *en;
    }
}
void yyy(BankAcc* acc) {
    acc->bal = acc->bal * sim_pow((1 + 0.1), 3);
}
int main() {
    BankAcc myAcc;
    initAcc(&myAcc, 9981, 2200.0);
    double amount = 100.0;
    xxx(&myAcc, &amount);
    yyy(&myAcc);
    printf("%d and %.2f", myAcc.accNum, myAcc.bal);
    return 0;
}
```

---

**Q12.** 다음 파이썬 코드에 대한 알맞은 출력 값을 작성하시오.

```python
a = ["Seoul", "Kyeonggi", "Incheon", "Daejun", "Daegu", "Pusan"]
str = "S"
for i in a:
    str = str + i[1]
print(str)
```

---

**Q13.** 다음 SQL 문장과 테이블을 참고하여 출력 값을 작성하시오.

```sql
SELECT B FROM R1
WHERE C IN (SELECT C FROM R2 WHERE D = 'k');
```

R1

| A | B | C |
|---|---|---|
| 1 | a | 1 |
| 2 | b | 2 |
| 3 | c | 3 |
| 4 | d | 4 |
| 5 | e | 5 |

R2

| C | D |
|---|---|
| 1 | k |
| 2 | m |
| 3 | k |

---

**Q14.** 다음 애플리케이션 테스트 관리에 대한 설명에 알맞은 답을 보기에서 골라 작성하시오.

1. 모든 분기와 조건의 조합을 고려하나 모든 조합을 테스트하는 대신 테스트가 필요한 중요한 조합을 찾아내는데 중점을 둔다.
2. 특정 조건을 수행할 때 다른 조건과는 상관없이 전체 결과에 영향을 미치는 조건만을 테스트한다.
3. 각각의 파라미터는 적어도 한 번은 최종 결과에 영향을 주어야 한다.

[보기] ㄱ.구문 커버리지 / ㄴ.결정 커버리지 / ㄷ.조건 커버리지 / ㄹ.변경 조건/결정 커버리지 / ㅁ.다중 조건 커버리지 / ㅂ.경로 커버리지 / ㅅ.조건/결정 커버리지

---

**Q15.** 다음 설명에 알맞은 보기의 용어를 골라 작성하시오.

인터넷 공격자의 존재를 숨기면서 이 공격자에게 시스템에 대한 무제한 접근 권한을 부여하는 악성 프로그램이다. 운영체제의 시스템콜을 해킹하여 악성코드의 실행여부를 숨겨 안티바이러스 탐지를 우회할 수 있다.

[보기] ㄱ.Worm / ㄴ.Trojan horse / ㄷ.Backdoor / ㄹ.Virus / ㅁ.Ransomware / ㅂ.Spyware / ㅅ.Rootkit

---

**Q16.** 다음 Java 코드를 보고 알맞은 출력 값을 작성하시오.

```java
class classOne {
    int a, b;
    public classOne(int a, int b) {
        this.a = a;
        this.b = b;
    }
    public void print() {
        System.out.println(a + b);
    }
}
class classTwo extends classOne {
    int po = 3;
    public classTwo(int i) {
        super(i, i + 1);
    }
    public void print() {
        System.out.println(po * po);
    }
}
public class main {
    public static void main(String[] args) {
        classOne one = new classTwo(10);
        one.print();
    }
}
```

---

**Q17.** 다음 설명에 알맞은 보기의 용어를 골라 작성하시오.

1. 불특정 다수가 아닌 명확한 표적을 정하여 지속적인 정보수집 후 공격을 감행한다.
2. 시스템에 직접 침투하는 것뿐 아니라 표적 내부 직원들이 이용하는 다양한 단말을 대상으로 한다.
3. Zero-day 취약점, 악성코드 등 다양한 보안 위협 공격 기술을 사용한다.
4. 일반적으로 침투, 검색, 수집 및 유출의 4단계로 실행된다.

[보기] ㄱ.사회공학 기법 / ㄴ.Adware / ㄷ.MITM / ㄹ.XDR / ㅁ.Replace attack / ㅂ.key logger attack / ㅅ.APT

---

**Q18.** 다음 SQL 코드의 결과 값을 작성하시오.

```sql
SELECT COUNT(*)
FROM TABLE
WHERE EMPNO > 100
AND SAL >= 3000 OR EMPNO = 200;
```

| EMPNO | SAL  |
|-------|------|
| 100   | 1000 |
| 200   | 3000 |
| 300   | 1500 |

---

**Q19.** 다음 C언어 코드의 알맞은 출력 값을 작성하시오.

```c
#include <stdio.h>
#include <ctype.h>
int main() {
    char* p = "It is 8";
    char result[100];
    int i;
    for (i = 0; p[i] != '\0'; i++) {
        if (isupper(p[i]))
            result[i] = (p[i] - 'A' + 5) % 25 + 'A';
        else if (islower(p[i]))
            result[i] = (p[i] - 'a' + 10) % 26 + 'a';
        else if (isdigit(p[i]))
            result[i] = (p[i] - '0' + 3) % 10 + '0';
        else
            result[i] = p[i];
    }
    result[i] = '\0';
    printf("%s\n", result);
    return 0;
}
```

---

**Q20.** 다음 설명에 알맞은 디자인 패턴의 이름을 작성하시오.

구체적인 클래스에 의존하지 않고 서로 연관되거나 의존적인 객체들의 조합을 만드는 인터페이스를 제공하는 패턴이다. 관련성 있는 여러 종류의 객체를 일관된 방식으로 생성하는 경우에 유용하며 kit라고도 불린다.

---
---

## 정답 모음

| 번호 | 정답 | 풀이 |
|------|------|------|
| Q01 | 4 | Singleton 패턴: Connection.get()은 항상 동일 인스턴스 반환. count()는 conn1×2+conn2×1+conn3×1=4번 호출 |
| Q02 | 151 | 삼항: (0>35 ? 35 : 0)=0 → false → else 실행: v3=29<<2=116. 출력: 35+116=151 |
| Q03 | ㄱ > ㄴ > ㄹ > ㄷ | 응집도 강→약: 기능>순차>교환>절차>시간>논리>우연. 기능(ㄱ)>교환(ㄴ)>시간(ㄹ)>우연(ㄷ) |
| Q04 | GECA | "ABCDEFGH" → reverse → "HGFEDCBA". i=1,3,5,7 → str[1]='G',str[3]='E',str[5]='C',str[7]='A' → GECA |
| Q05 | 2번: 192.168.35.72 / 4번: 129.200.8.249 / 5번: 192.168.36.249 | /24 서브넷: .35.72는 .35.x 대역, /22→네트워크 129.200.8.0 내 .8.249 가능, .36.249는 .36.x 대역 |
| Q06 | 3NF (제3정규형) | 1NF·2NF 모두 만족, 이행 함수 종속 없음 → 3NF |
| Q07 | OSPF (Open Shortest Path First) | 링크 상태 프로토콜. IGP. Dijkstra 알고리즘으로 최단 경로 계산 |
| Q08 | (1) 세타 조인 (Theta Join) / (2) 동등 조인 (Equi Join) / (3) 자연 조인 (Natural Join) | 세타=비교연산자 조건, 동등=등호(=) 조건, 자연=동등+중복 속성 제거 |
| Q09 | (1) LRU: 6 / (2) LFU: 6 | LRU: 1,2,3 miss→1,2 hit→4 miss→1,2 hit→5,7 miss=6회. LFU: 동일한 교체 패턴으로 6회 |
| Q10 | 6 → 7 → 4 → 1 → 2 → 3 | main(⑥)→new Child(3)(⑦)→Child생성자(④)→super()(①)→Parent생성자 실행(②)→parent.getT()(③) |
| Q11 | 9981 and 2795.10 | initAcc: accNum=9981, bal=2200. xxx: 100>0 && 100<2200 → bal=2200-100=2100. yyy: 2100×1.1³=2100×1.331=2795.1 |
| Q12 | Seynaau | str="S". 각 도시 2번째 문자: Seoul[1]='e', Kyeonggi[1]='y', Incheon[1]='n', Daejun[1]='a', Daegu[1]='a', Pusan[1]='u' → "Seynaau" |
| Q13 | a / c | R2에서 D='k'인 C값: {1,3}. R1에서 C IN(1,3)인 B값: B=a(C=1), B=c(C=3) |
| Q14 | 1: ㄹ (변경 조건/결정 커버리지) / 2: ㄹ (변경 조건/결정 커버리지) / 3: ㄹ (변경 조건/결정 커버리지) | MC/DC: 각 조건이 독립적으로 전체 결과에 영향을 주는 조합을 테스트. 항공·안전 분야 필수 |
| Q15 | ㅅ (Rootkit) | 루트킷: 공격자 존재 은닉 + 무제한 권한 부여. 커널 레벨에서 시스템콜 후킹으로 탐지 우회 |
| Q16 | 9 | new classTwo(10): super(10,11)→a=10,b=11,po=3. one.print()→classTwo.print()(오버라이딩)→po×po=9 |
| Q17 | ㅅ (APT, Advanced Persistent Threat) | 지능형 지속 공격. 특정 표적·다단계·다양한 기술 사용. 침투→검색→수집→유출 4단계 |
| Q18 | 1 | 우선순위: AND > OR. (EMPNO>100 AND SAL>=3000) OR EMPNO=200. 200행만 조건 충족 → COUNT=1 |
| Q19 | Nd sc 1 | I→N(8+5=13,'A'+13), t→d(19+10=29,29%26=3,'a'+3), 공백→공백, i→s(8+10=18), s→c(18+10=28,28%26=2), 공백→공백, 8→1(8+3=11,11%10=1) |
| Q20 | Abstract Factory (추상 팩토리) 패턴 | 연관 객체 군을 일관되게 생성. 구체 클래스 의존 없음. kit 이름으로도 불림. GoF 생성 패턴 |
