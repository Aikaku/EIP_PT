# 2020년 정보처리기사 실기 기출문제 — 3회

> 총 20문제

---

**Q01.** 리팩토링의 목적에 대한 설명을 작성하시오.

---

**Q02.** 다음의 출력 결과를 쓰시오.

```c
#include <stdio.h>
void main() {
    int c = 0;
    int i = 0;
    while(i < 10) {
        i++;
        c *= i;
    }
    printf("%d", c);
}
```

---

**Q03.** 다음 학생 테이블에서 이름이 민수인 튜플을 삭제하는 SQL문을 작성하시오.
- 테이블명: 학생, 필드명: 이름

---

**Q04.** TCP/IP에서 신뢰성없는 IP를 대신하여 송신측으로 네트워크의 IP 상태 및 에러 메시지를 전달해주는 프로토콜을 ( ) 이라 한다. 괄호 안에 들어갈 용어를 쓰시오.

---

**Q05.** 다음의 출력 결과를 쓰시오.

```java
public class Test {
    public static void main(String[] args) {
        int i = 0;
        int sum = 0;
        while (i < 10) {
            i++;
            if(i % 2 == 1)
                continue;
            sum += i;
        }
        System.out.print(sum);
    }
}
```

---

**Q06.** 심리학자 톰 마릴은 컴퓨터가 메시지를 전달하고 메시지가 제대로 도착했는지 확인하며 도착하지 않았을 경우 메시지를 재전송하는 일련의 방법을 '기술적 은어'를 뜻하는 ( )이라는 용어로 정의하였다. 괄호 안에 들어갈 용어는?

---

**Q07.** 다음 아래 제어 흐름 그래프가 **분기 커버리지**를 만족하기 위한 테스팅 순서를 쓰시오.

```
① → ② → ③ --YES→ ④ → ⑤ → ⑥ → ②
              |
              NO→ ⑦
```

---

**Q08.** 다음 조건을 만족하면서, 과목별 점수의 평균이 90 이상인 과목이름, 최소점수, 최대점수를 구하는 SQL문을 작성하시오.

- WHERE 구문을 사용하지 않는다.
- GROUP BY, HAVING 구문을 반드시 사용한다.
- 별칭(AS)을 사용해야 한다.

[성적 테이블]

| 과목코드 | 과목이름 | 학점 | 점수 |
|---------|---------|------|------|
| 1000 | 컴퓨터과학 | A+ | 95 |
| 2000 | 운영체제 | B+ | 85 |
| 1000 | 컴퓨터과학 | B+ | 85 |
| 2000 | 운영체제 | B | 80 |

---

**Q09.** 다음 학생 테이블에서 이름이 민수인 튜플을 삭제하는 SQL문을 작성하시오.

| 학번 | 이름 | 점수 | 과목이름 |
|------|------|------|---------|
| 1000 | 김정미 | 90 | 알고리즘 |
| 2000 | 강은미 | 95 | 데이터베이스 |
| 3000 | 홍길동 | 90 | 전산수학 |
| 4000 | 민수 | 95 | 운영체제 |

---

**Q10.** 릴레이션 A, B가 있을 때 릴레이션 B 조건에 맞는 것들만 릴레이션 A에서 튜플을 꺼내 프로젝션하는 관계대수의 기호는 무엇인가?

---

**Q11.** 헝가리안 표기법(Hungarian Notation)에 대해서 서술하시오.

---

**Q12.** 테스트의 종류 중, 동치 분할 테스트, 경계값 분석 테스트 등의 종류가 있는 테스트 기법을 쓰시오.

---

**Q13.** 다음은 C언어 소스 코드이다. 출력 결과를 쓰시오.

```c
#include <stdio.h>
int r1() { return 4; }
int r10() { return (30 + r1()); }
int r100() { return (200 + r10()); }
int main() {
    printf("%d\n", r100());
    return 0;
}
```

---

**Q14.** 데이터베이스에서 스키마(Schema)에 대해 간략히 설명하시오.

---

**Q15.** 다음은 자바(Java) 코드이다. 출력 결과를 쓰시오.

```java
abstract class vehicle {
    private String name;
    abstract public String getName(String val);
    public String getName() {
        return "vehicle name:" + name;
    }
    public void setName(String val) { name = val; }
}
class Car extends vehicle {
    public Car(String val) { setName(val); }
    public String getName(String val) { return "Car name : " + val; }
    public String getName(byte val[]) { return "Car name : " + val; }
}
public class good {
    public static void main(String[] args) {
        vehicle obj = new Car("Spark");
        System.out.print(obj.getName());
    }
}
```

---

**Q16.** UI 설계 원칙 중 직관성에 대해 설명하시오.

---

**Q17.** 다음은 자바(Java) 코드이다. 출력 결과를 쓰시오.

```java
public class Test {
    public static void main(String[] args) {
        int i = 0;
        int sum = 0;
        while (i < 10) {
            i++;
            if(i % 2 == 1)
                continue;
            sum += i;
        }
        System.out.print(sum);
    }
}
```

---

**Q18.** EAI 유형에는 메세지 버스(Message bus) 유형, 하이브리드(Hybrid) 유형, (A) 유형, (B) 유형이 있다. 괄호 안에 들어갈 말을 적으시오.

---

**Q19.** C++에서 생성자란 무엇인지 쓰시오.

---

**Q20.** 학생 테이블에 주소 속성을 추가하는 SQL문을 작성하려고 한다. 빈 칸을 채우시오.

```sql
( A ) TABLE 학생 ( B ) 주소 VARCHAR(20);
```

---
---

## 정답 모음

| 번호 | 정답 | 풀이 |
|------|------|------|
| Q01 | 소프트웨어를 보다 이해하기 쉽고 수정하기 쉽도록 코드의 내부 구조를 재조정하는 것 (결과의 변경 없이 가독성·유지보수성 향상) | 외부 동작은 바꾸지 않고 내부 구조만 개선. 기술 부채 해소, 확장성 확보가 주목적 |
| Q02 | `0` | c=0에서 시작. c*=i → 0×1=0, 0×2=0 ... 어떤 수를 곱해도 0 유지 |
| Q03 | `DELETE FROM 학생 WHERE 이름 = '민수';` | DML 삭제문. FROM 절에 테이블명, WHERE 절에 조건 지정. 조건 없으면 전체 삭제 주의 |
| Q04 | ICMP (Internet Control Message Protocol) | 네트워크 계층(3계층). IP의 신뢰성 없는 전달을 보완하여 오류 메시지·상태 정보를 전달 (ping, traceroute에 사용) |
| Q05 | `30` | i가 1→10 증가 시 홀수면 continue(skip). 짝수(2,4,6,8,10)만 sum에 누적 → 2+4+6+8+10=30 |
| Q06 | 프로토콜(Protocol) | 톰 마릴이 정의한 컴퓨터 간 통신 규약. 구문(Syntax)·의미(Semantics)·시간(Timing) 3요소로 구성 |
| Q07 | `1→2→3→4→5→6→2→7` | 분기 커버리지: YES/NO 두 분기를 모두 실행해야 함. YES 경로(루프 진입)+NO 경로(⑦로 빠짐) 모두 포함 |
| Q08 | `SELECT 과목이름, MIN(점수) AS 최소점수, MAX(점수) AS 최대점수 FROM 성적 GROUP BY 과목이름 HAVING AVG(점수) >= 90` | WHERE 대신 HAVING으로 그룹 조건 지정. 컴퓨터과학 평균=(95+85)/2=90 → 조건 충족, 운영체제 평균=(85+80)/2=82.5 → 제외 |
| Q09 | `DELETE FROM 학생 WHERE 이름 = '민수';` | Q03과 동일 형태. 학번 4000, 이름 민수인 튜플 삭제 |
| Q10 | `÷` (Division, 관계 대수 나누기) | 릴레이션 B의 모든 튜플과 대응되는 A 튜플만 추출. SQL의 `NOT EXISTS`로 구현 가능 |
| Q11 | 식별자 표기 시 접두어에 자료형을 붙이는 표기법 | 예: `strName`(String), `nCount`(int), `bFlag`(bool). 가독성 향상, IDE 자동완성 전 시대에 유용했음 |
| Q12 | 블랙박스 테스트 (명세 기반 테스트) | 내부 구현 무관, 입출력 명세 기반 테스트. 동치 분할·경계값 분석·결정 테이블 등이 대표 기법 |
| Q13 | `234` | r1()=4, r10()=30+r1()=34, r100()=200+r10()=234. 순차 함수 호출 추적 |
| Q14 | 데이터베이스의 전체적인 구조와 제약조건에 대한 명세로, 내부 스키마·개념 스키마·외부 스키마로 구분됨 | 외부=사용자 뷰, 개념=전체 논리 구조, 내부=물리적 저장 구조. 3단계 스키마 구조(ANSI/SPARC) |
| Q15 | `vehicle name:Spark` | obj 타입은 vehicle이므로 인수 없는 `getName()` 호출 → vehicle의 `getName()` 실행 → name은 setName("Spark")으로 설정됨 |
| Q16 | 누구나 쉽게 이해하고, 쉽게 사용할 수 있어야 한다는 원칙 | UI 설계 4원칙 중 직관성. 별도 학습 없이도 즉시 사용 가능한 인터페이스 지향 |
| Q17 | `30` | Q05와 동일한 코드 → 동일 결과 30 |
| Q18 | A: Point-to-Point / B: Hub & Spoke | EAI 4유형: 포인트투포인트(1:1 직접 연결), 허브앤스포크(중앙 허브 경유), 메시지버스(공유 버스), 하이브리드(혼합) |
| Q19 | 해당 클래스의 객체가 생성될 때 자동으로 호출되는 특수한 종류의 메서드 | 클래스명과 동일, 반환 타입 없음. 객체 초기화 담당. C++에서 소멸자(Destructor)와 쌍을 이룸 |
| Q20 | A: `ALTER` / B: `ADD` | DDL 테이블 변경문. `ALTER TABLE 테이블명 ADD 컬럼명 자료형`. 컬럼 삭제는 DROP, 수정은 MODIFY |
