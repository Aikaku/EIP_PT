# 8장 SQL 응용 — 다이어그램 학습

---

## 전체 구조 마인드맵

```mermaid
mindmap
  root((8장 SQL))
    DDL 데이터 정의어
      CREATE 생성
      ALTER 변경
      DROP 삭제
      TRUNCATE
    DML 데이터 조작어
      SELECT 조회
      INSERT 삽입
      UPDATE 수정
      DELETE 삭제
    DCL 데이터 제어어
      COMMIT
      ROLLBACK
      GRANT
      REVOKE
    SELECT 옵션
      DISTINCT
      WHERE 조건
      GROUP BY
      HAVING
      ORDER BY
    JOIN
      INNER EQUI NATURAL
      OUTER LEFT RIGHT FULL
    그룹함수
      COUNT SUM AVG
      MAX MIN
    트리거 Trigger
```

---

## DDL 명령어 ★A

```mermaid
graph TD
    DDL["📐 DDL Data Definition Language\n데이터 정의어\n구조 생성·변경·삭제\nAUTO COMMIT"]

    DDL --> CR["CREATE\n테이블·인덱스·뷰 생성"]
    DDL --> AL["ALTER\n테이블 구조 변경"]
    DDL --> DR["DROP\n테이블·인덱스·뷰 삭제"]
    DDL --> TR["TRUNCATE\n테이블 데이터 전체 삭제\n구조는 유지 ROLLBACK 불가"]

    CR --> CR1["CREATE TABLE 테이블명\n  (컬럼명 데이터타입 제약조건)"]
    CR --> CR2["CREATE INDEX 인덱스명 ON 테이블명(컬럼)\nUNIQUE·ASC/DESC·CLUSTER 옵션"]

    AL --> AL1["ALTER TABLE 테이블명\n  ADD 컬럼명 데이터타입"]
    AL --> AL2["ALTER TABLE 테이블명\n  ALTER COLUMN 컬럼명 타입\n  = MODIFY (Oracle)"]
    AL --> AL3["ALTER TABLE 테이블명\n  DROP COLUMN 컬럼명"]

    DR --> DR1["DROP TABLE 테이블명 CASCADE\n참조 테이블도 함께 삭제"]
    DR --> DR2["DROP TABLE 테이블명 RESTRICT\n참조 중이면 삭제 거부"]

    style DDL fill:#cce5ff,stroke:#004085
    style CR fill:#d4edda
    style AL fill:#fff3cd
    style DR fill:#f8d7da
    style TR fill:#e2d9f3
```

---

## DCL 명령어 ★A

```mermaid
graph TD
    DCL["🔐 DCL Data Control Language\n데이터 제어어\n접근 권한·트랜잭션 제어"]

    DCL --> TC["트랜잭션 제어"]
    DCL --> AC["접근 제어"]

    TC --> COM["COMMIT\n트랜잭션 작업 완료 확정\nDB에 영구 반영"]
    TC --> ROL["ROLLBACK\n트랜잭션 취소\n이전 COMMIT 상태로 복귀"]
    TC --> SAV["SAVEPOINT\n트랜잭션 중간 저장점 설정\n부분 ROLLBACK 가능"]

    AC --> GR["GRANT 권한 ON 객체 TO 사용자\n권한 부여\nWITH GRANT OPTION: 재부여 가능"]
    AC --> RE["REVOKE 권한 ON 객체 FROM 사용자\n권한 회수\nCASCADE: 연쇄 회수"]

    style DCL fill:#fff3cd
    style TC fill:#d4edda
    style AC fill:#f8d7da
    style COM fill:#e8f4fd
    style ROL fill:#e8f4fd
    style GR fill:#fce8e8
    style RE fill:#fce8e8
```

---

## SELECT 문 완전 구조 ★A

```mermaid
graph TD
    SEL["SELECT 완전 구조"]

    SEL --> S1["SELECT [DISTINCT] 컬럼명...\n  FROM 테이블명\n  [WHERE 조건]\n  [GROUP BY 컬럼명]\n  [HAVING 그룹조건]\n  [ORDER BY 컬럼명 ASC|DESC]"]

    S1 --> ORDER["실행 순서\nFROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY"]

    SEL --> COND["조건 연산자"]
    COND --> C1["LIKE '%문자%'\n% : 0개 이상 임의 문자\n_ : 정확히 1개 문자\n# : 숫자 1개 (MS-Access)"]
    COND --> C2["IN (값1, 값2)\n목록 중 해당 값"]
    COND --> C3["BETWEEN A AND B\nA 이상 B 이하"]
    COND --> C4["IS NULL / IS NOT NULL\nNULL 여부 확인"]

    style SEL fill:#cce5ff
    style ORDER fill:#f8d7da
    style C1 fill:#d4edda
    style C2 fill:#d4edda
    style C3 fill:#d4edda
    style C4 fill:#d4edda
```

---

## JOIN 종류 ★A

```mermaid
graph TD
    JOIN["🔗 JOIN\n두 테이블을 결합해\n하나의 결과 반환"]

    JOIN --> IJ["INNER JOIN\n교집합 = 두 테이블\n공통 부분만 반환"]
    IJ --> TJ["THETA JOIN\n임의 조건으로 조인"]
    IJ --> EQ["EQUI JOIN ★\n= 로 조인\n가장 일반적"]
    IJ --> NT["NATURAL JOIN\n동일 이름 컬럼\n자동으로 조인\n중복 컬럼 1개만 표시"]

    JOIN --> OJ["OUTER JOIN\n한쪽에만 있어도 반환\nNULL로 채움"]
    OJ --> LO["LEFT OUTER JOIN\n왼쪽 테이블 전체 반환"]
    OJ --> RO["RIGHT OUTER JOIN\n오른쪽 테이블 전체 반환"]
    OJ --> FO["FULL OUTER JOIN\n양쪽 테이블 전체 반환"]

    JOIN --> CJ["CROSS JOIN = CARTESIAN\n카티전 곱\n모든 행 조합 반환"]

    style IJ fill:#d4edda
    style OJ fill:#cce5ff
    style EQ fill:#d4edda,stroke:#28a745
    style LO fill:#e8f4fd
    style RO fill:#e8f4fd
    style FO fill:#e8f4fd
    style CJ fill:#f8d7da
```

---

## 그룹 함수 ★A

```mermaid
graph LR
    subgraph AGG["집계 함수 Aggregate Functions"]
        A1["COUNT(*)\n전체 행 수\nNULL 포함"]
        A2["COUNT(컬럼)\nNULL 제외한 행 수"]
        A3["SUM(컬럼)\n합계\n숫자형만"]
        A4["AVG(컬럼)\n평균\n숫자형만"]
        A5["MAX(컬럼)\n최댓값"]
        A6["MIN(컬럼)\n최솟값"]
        A7["STDDEV(컬럼)\n표준편차"]
        A8["VARIANCE(컬럼)\n분산"]
    end

    style AGG fill:#fff3cd
```

```sql
-- 사용 예시
SELECT 부서, COUNT(*) AS 인원수, AVG(급여) AS 평균급여
FROM 사원
GROUP BY 부서
HAVING COUNT(*) >= 3
ORDER BY 평균급여 DESC;
```

---

## 집합 연산자 ★B

```mermaid
graph LR
    subgraph SET_OP["집합 연산자"]
        U["UNION\n합집합\n중복 제거"]
        UA["UNION ALL\n합집합\n중복 포함"]
        I["INTERSECT\n교집합\n공통 행만"]
        E["EXCEPT / MINUS\n차집합\n첫 번째에만 있는 행"]
    end

    style U fill:#d4edda
    style UA fill:#d4edda
    style I fill:#cce5ff
    style E fill:#f8d7da
```

---

## 트리거(Trigger) ★A

```mermaid
sequenceDiagram
    participant U as 사용자
    participant DB as DB
    participant TR as 트리거

    Note over TR: 이벤트 발생 시\n자동 실행되는 절차형 SQL

    U->>DB: INSERT/UPDATE/DELETE 실행

    DB->>TR: BEFORE 트리거 실행
    Note over TR: 작업 전 실행

    DB->>DB: 실제 데이터 변경

    DB->>TR: AFTER 트리거 실행
    Note over TR: 작업 후 실행\n로그 기록·다른 테이블 연동

    TR-->>DB: 연쇄 작업 수행
```

```sql
-- 트리거 기본 구조
CREATE TRIGGER 트리거명
  AFTER INSERT ON 테이블명
  FOR EACH ROW
BEGIN
  -- 실행할 SQL
END;
```

---

## 하위질의(서브쿼리) ★A

```mermaid
graph TD
    SQ["📌 하위질의 Subquery\nSELECT 안에 포함된 SELECT\n괄호로 묶어서 사용"]

    SQ --> S1["단일 행 서브쿼리\n결과가 1행\n=·>·<·>=·<= 사용"]
    SQ --> S2["다중 행 서브쿼리\n결과가 여러 행\nIN·ANY·ALL·EXISTS 사용"]
    SQ --> S3["다중 열 서브쿼리\n결과가 여러 열"]

    S2 --> O1["IN: 목록 중 하나라도 일치"]
    S2 --> O2["ANY: 하나라도 만족"]
    S2 --> O3["ALL: 모두 만족"]
    S2 --> O4["EXISTS: 결과 행 존재 여부"]

    style SQ fill:#fff3cd
    style S1 fill:#d4edda
    style S2 fill:#cce5ff
    style S3 fill:#f8d7da
```

---

## 핵심 암기 요약표

| 번호 | 항목 | 핵심 키워드 | 난이도 |
|------|------|-------------|--------|
| 120 | DDL 종류 | CREATE·ALTER·DROP·TRUNCATE (AUTO COMMIT) | **A** |
| 121 | DROP CASCADE vs RESTRICT | CASCADE=연쇄삭제 / RESTRICT=참조시거부 | **A** |
| 122 | COMMIT | 트랜잭션 완료 확정, 영구 반영 | **A** |
| 123 | ROLLBACK | 트랜잭션 취소, 이전 COMMIT 복귀 | **A** |
| 124 | GRANT | GRANT 권한 ON 객체 TO 사용자 | **A** |
| 125 | REVOKE | REVOKE 권한 ON 객체 FROM 사용자 | **A** |
| 126 | SELECT 실행 순서 | FROM→WHERE→GROUP BY→HAVING→SELECT→ORDER BY | **A** |
| 127 | LIKE % vs _ | %=0개이상 / _=정확히1개 | **A** |
| 128 | EQUI JOIN | = 연산자로 조인, 가장 일반적 | **A** |
| 129 | NATURAL JOIN | 같은 이름 컬럼 자동 조인, 중복 제거 | **B** |
| 130 | LEFT OUTER JOIN | 왼쪽 전체 + 오른쪽 NULL | **A** |
| 131 | COUNT(*) vs COUNT(컬럼) | *=NULL포함 / 컬럼=NULL제외 | **A** |
| 132 | UNION vs UNION ALL | UNION=중복제거 / UNION ALL=중복포함 | **A** |
| 133 | INTERSECT | 교집합, 두 테이블 공통 행 | **B** |
| 134 | 트리거 | 이벤트 발생 시 자동 실행 절차형 SQL | **A** |
| 135 | 서브쿼리 다중행 | IN·ANY·ALL·EXISTS | **B** |

---

*8장 SQL 응용 (실기_이론(1) p.8 기반)*
