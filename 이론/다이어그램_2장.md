# 2장 데이터 입출력 구현 — 다이어그램 학습

---

## 전체 구조 마인드맵

```mermaid
mindmap
  root((2장 DB))
    DB 설계
      034 스키마 3계층 A
      035 설계 5단계 A
      036 개념적 설계 A
      037 논리적 설계 A
      038 물리적 설계 A
      039 데이터 모델 A
    릴레이션 구조
      040 E-R 다이어그램 B
      041 릴레이션 구조 A
      042 튜플 카디널리티 A
      043 속성 디그리 A
      044 도메인 A
    키 종류
      045 후보키 유일성최소성 A
      046 대체키 보조키 A
      047 슈퍼키 유일성만 A
      기본키 PK A
      048 외래키 FK A
    049 무결성 A
    관계대수 연산
      050 관계대수 B
      051 순수관계연산자 A
      052 일반집합연산자 A
      053 관계해석 비절차 A
    정규화
      054 이상 Anomaly A
      055 함수적 종속 A
      057 정규화 과정 A
      058 정규화 단계 정리 B
      059 반정규화 A
    트랜잭션
      061 ACID 특성 A
      062 CRUD 분석 C
    DB 관리
      060 시스템 카탈로그 B
      063 인덱스 C
      064 뷰 B
      065 파티션 종류 B
      066 분산DB 목표 B
      067 RTO RPO A
    접근통제
      068 DAC 임의 A
      069 MAC 강제 A
      070 RBAC 역할기반 A
    자료 구조
      073 선형 비선형 B
      074 스택 LIFO B
      076 트리 용어 B
      077 Preorder B
      078 Inorder B
    정렬 알고리즘
      082 선택 정렬 B
      083 버블 정렬 B
      084 퀵 정렬 B
```

---

## 스키마 3계층 ★A

```mermaid
graph TD
    subgraph USER["👤 사용자 관점"]
        ES["외부 스키마\nExternal Schema\n사용자/응용 프로그램 입장\n뷰(View)에 해당\n여러 개 존재 가능"]
    end

    subgraph DESIGN["🏗️ 설계자 관점"]
        CS["개념 스키마\nConceptual Schema\n전체 DB 논리적 구조\n개체·관계·제약조건\n1개 존재"]
    end

    subgraph DBA["⚙️ DBA 관점"]
        IS["내부 스키마\nInternal Schema\n물리적 저장 구조\n레코드 형식·저장 방법\n1개 존재"]
    end

    ES -->|외부/개념 매핑| CS
    CS -->|개념/내부 매핑| IS

    style ES fill:#cce5ff
    style CS fill:#d4edda
    style IS fill:#fff3cd
```

---

## DB 설계 5단계 ★A

```mermaid
sequenceDiagram
    participant REQ as 요구사항 분석
    participant CON as 개념적 설계
    participant LOG as 논리적 설계
    participant PHY as 물리적 설계
    participant IMP as 구현

    Note over REQ: 업무 분석\n데이터 요구사항 수집

    REQ->>CON: 요구사항 명세서
    Note over CON: E-R 다이어그램 작성\n개념 스키마 설계\nDBMS 독립적

    CON->>LOG: E-R 다이어그램
    Note over LOG: 릴레이션 스키마 변환\n정규화 수행\nDBMS에 종속

    LOG->>PHY: 릴레이션 스키마
    Note over PHY: 인덱스·저장 구조 설계\n접근 경로 설계\n물리적 구조 정의

    PHY->>IMP: 물리적 설계 명세서
    Note over IMP: SQL로 DB 생성\n트랜잭션 구현
```

---

## 개념적·논리적·물리적 설계 ★A

```mermaid
graph TD
    D1["개념적 설계 036 ★\n정보 모델링·개념화\nDBMS 독립적\nE-R 다이어그램 작성\n개념 스키마 모델링 + 트랜잭션 모델링"]
    D2["논리적 설계 037 ★\n데이터 모델링\n특정 DBMS에 종속\n논리 스키마 설계\n정규화 수행\n릴레이션 스키마로 변환 매핑"]
    D3["물리적 설계 038 ★\n데이터 구조화\n물리적 저장장치에 맞게 변환\n저장 구조·접근 경로 설계\n파일의 저장 구조 및 액세스 경로 결정"]

    D1 --> D2 --> D3

    style D1 fill:#d4edda,stroke:#28a745
    style D2 fill:#cce5ff,stroke:#004085
    style D3 fill:#fff3cd,stroke:#f0c000
```

---

## 데이터 모델 3요소 ★A

```mermaid
graph TD
    DM["📐 데이터 모델 039\n현실 세계의 정보들을\n단순화·추상화하여 표현한 개념적 모형"]

    DM --> S["구조 Structure\n논리적으로 표현된\n개체 타입들 간의 관계\n데이터 구조 및 정적 성질 표현"]
    DM --> O["연산 Operation\nDB에 저장된 실제 데이터를\n처리하는 작업 명세\nDB를 조작하는 기본 도구"]
    DM --> C["제약 조건 Constraint\nDB에 저장될 수 있는\n실제 데이터의 논리적 제약 조건"]

    style DM fill:#fff3cd
    style S fill:#d4edda
    style O fill:#cce5ff
    style C fill:#f8d7da
```

---

## E-R 다이어그램 기호 ★B

```mermaid
graph LR
    subgraph ER["E-R 다이어그램 기호 040"]
        E["□ 사각형\n개체 Entity 타입"]
        R["◇ 마름모\n관계 Relationship 타입"]
        A["○ 타원\n속성 Attribute"]
        DA["이중 타원\n다중값 속성(복합속성)"]
        UA["밑줄 타원\n기본키 속성"]
        LA["─ 선·링크\n개체 타입과 속성 연결"]
    end

    style E fill:#cce5ff
    style R fill:#fff3cd
    style A fill:#d4edda
    style DA fill:#f8d7da
    style UA fill:#e2d9f3
```

---

## 릴레이션 구조 + 용어 ★A

```mermaid
graph TD
    REL["📊 릴레이션 구조 041\n데이터들을 표 Table 형태로 표현"]

    REL --> SC["릴레이션 스키마\n구조 정의 (헤더)"]
    REL --> INS["릴레이션 인스턴스\n실제 데이터 값들"]

    REL --> TUP["튜플 Tuple 042 ★\n릴레이션의 각각의 행 Row\n파일 구조의 레코드와 같음\n튜플의 수 = 카디널리티 Cardinality"]
    REL --> ATT["속성 Attribute 043 ★\nDB를 구성하는 가장 작은 논리적 단위\n파일 구조의 데이터 필드\n속성의 수 = 디그리 Degree(차수)"]
    REL --> DOM["도메인 Domain 044 ★\n하나의 속성이 취할 수 있는\n같은 타입의 원자값들의 집합\n예: 성별 도메인 = {남, 여}"]

    style REL fill:#cce5ff
    style TUP fill:#d4edda
    style ATT fill:#fff3cd
    style DOM fill:#f8d7da
```

---

## 키(Key) 종류 ★A

```mermaid
graph TD
    TUPLE["릴레이션의 튜플"]

    TUPLE --> SK["슈퍼키 Super Key 047 ★\n유일성 O / 최소성 X\n속성 집합으로 튜플 구분 가능"]
    SK --> CK["후보키 Candidate Key 045 ★\n유일성 O + 최소성 O\n기본키가 될 수 있는 후보"]
    CK --> PK["기본키 Primary Key ★\n후보키 중 선정된 키\nNULL 불가 / 중복 불가"]
    CK --> AK["대체키 Alternate Key 046 ★\n기본키로 선정되지 않은 후보키\n보조키라고도 함"]
    CK --> FK["외래키 Foreign Key 048 ★\n다른 릴레이션의 기본키 참조\n참조 무결성 유지\n참조 기본키에 없는 값 입력 불가"]

    style PK fill:#f8d7da,stroke:#dc3545
    style FK fill:#cce5ff,stroke:#004085
    style CK fill:#d4edda
    style SK fill:#fff3cd
    style AK fill:#e2d9f3
```

---

## 무결성 제약조건 ★A

```mermaid
graph TD
    INT["🛡️ 무결성 Integrity"]

    INT --> E_INT["개체 무결성\nEntity Integrity\n기본키는 NULL이거나\n중복값을 가질 수 없음"]
    INT --> R_INT["참조 무결성\nReferential Integrity\n외래키는 참조 릴레이션의\n기본키 값이거나 NULL"]
    INT --> D_INT["도메인 무결성\nDomain Integrity\n각 속성값은 정의된\n도메인에 속해야 함"]

    style INT fill:#fff3cd
    style E_INT fill:#d4edda
    style R_INT fill:#cce5ff
    style D_INT fill:#f8d7da
```

---

## 이상(Anomaly)과 정규화 ★A

```mermaid
graph TD
    ANOM["⚠️ 이상 Anomaly\n불필요한 데이터 중복으로\n발생하는 부작용"]

    ANOM --> A1["삽입 이상\nInsertion Anomaly\n불필요한 데이터 함께\n삽입해야 하는 현상"]
    ANOM --> A2["삭제 이상\nDeletion Anomaly\n의도치 않은 데이터\n함께 삭제되는 현상"]
    ANOM --> A3["갱신 이상\nUpdate Anomaly\n일부만 갱신되어\n불일치 발생하는 현상"]

    ANOM -->|해결| NORM["📐 정규화 Normalization\n함수적 종속성을 이용해\n릴레이션을 분해"]

    NORM --> NF1["1NF\n원자값만 허용\n반복 그룹 제거"]
    NF1 --> NF2["2NF\n부분 함수 종속 제거\n기본키 전체에 완전 종속"]
    NF2 --> NF3["3NF\n이행 함수 종속 제거\nA→B, B→C이면 A→C 제거"]
    NF3 --> BCNF["BCNF\n결정자이면서 후보키가 아닌\n함수 종속 제거"]
    BCNF --> NF4["4NF\n다중값 종속 제거"]
    NF4 --> NF5["5NF\n조인 종속성 이용\n모든 조인 종속 제거"]

    style ANOM fill:#f8d7da
    style NORM fill:#d4edda
    style NF1 fill:#e8f4fd
    style NF2 fill:#e8f4fd
    style NF3 fill:#e8f4fd
    style BCNF fill:#fff3cd,stroke:#f0c000
```

---

## 트랜잭션 ACID ★A

```mermaid
graph LR
    subgraph ACID["⚡ 트랜잭션 ACID 성질"]
        A["원자성\nAtomicity\n모두 실행되거나\n전혀 실행되지 않음\nCOMMIT / ROLLBACK"]
        C["일관성\nConsistency\n트랜잭션 수행 전후\nDB 상태 일관 유지"]
        I["격리성\nIsolation\n수행 중 다른 트랜잭션\n접근 불가\n독립성/순차성"]
        D["영속성\nDurability\n완료된 트랜잭션은\n영구 반영\n회복 기법으로 보장"]
    end

    style A fill:#f8d7da
    style C fill:#d4edda
    style I fill:#cce5ff
    style D fill:#fff3cd
```

---

## 함수적 종속 ★A

```mermaid
graph TD
    FD["📐 함수적 종속 Functional Dependency 055\nX → Y\n속성 X 값에 대해 항상 속성 Y 값이 하나만 연관"]

    FD --> F1["완전 함수적 종속\nFull Functional Dependency ★\n기본키 전체에 대해 종속\n어떤 부분 집합에도 종속 아님\n2NF에서 제거 대상"]
    FD --> F2["부분 함수적 종속\nPartial Functional Dependency ★\n기본키의 일부에 대해 종속\n기본키가 복합키일 때 발생\n→ 2NF로 제거"]
    FD --> F3["이행적 함수적 종속\nTransitive Functional Dependency ★\nX→Y이고 Y→Z이면 X→Z 성립\n→ 3NF로 제거"]

    style FD fill:#fff3cd
    style F1 fill:#d4edda,stroke:#28a745
    style F2 fill:#f8d7da
    style F3 fill:#cce5ff
```

---

## 관계대수 연산자 ★B

```mermaid
graph TD
    RA["📐 관계대수 Relational Algebra\n관계 DB에서 원하는 정보를\n기술하는 절차적 언어"]

    RA --> PURE["순수 관계 연산자"]
    RA --> GEN["일반 집합 연산자"]

    PURE --> P1["σ 셀렉트 Select\n조건에 맞는 튜플 반환\n=WHERE절"]
    PURE --> P2["π 프로젝트 Project\n특정 속성만 반환\n=SELECT절"]
    PURE --> P3["⋈ 조인 Join\n공통 속성으로 결합"]
    PURE --> P4["÷ 디비전 Division\n나눗셈 연산"]

    GEN --> G1["∪ 합집합 Union"]
    GEN --> G2["∩ 교집합 Intersection"]
    GEN --> G3["− 차집합 Difference"]
    GEN --> G4["× 카티전 곱 Cartesian"]

    style PURE fill:#d4edda
    style GEN fill:#cce5ff
```

---

## 일반 집합 연산자 ★A

```mermaid
graph TD
    GEN["📐 일반 집합 연산자 052\n일반 집합 이론 기반"]

    GEN --> U["∪ 합집합 UNION ★\n두 릴레이션의 합집합\n중복 튜플 제거\n카디널리티 ≤ 두 합"]
    GEN --> I["∩ 교집합 INTERSECTION ★\n두 릴레이션의 교집합\n공통 튜플만 반환"]
    GEN --> D["− 차집합 DIFFERENCE ★\n첫 번째에만 있는 튜플"]
    GEN --> CP["× 교차곱 CARTESIAN PRODUCT ★\n두 릴레이션 튜플의 모든 순서쌍\n디그리 = 두 디그리의 합\n카디널리티 = 두 카디널리티의 곱"]

    style U fill:#d4edda
    style I fill:#cce5ff
    style D fill:#f8d7da
    style CP fill:#fff3cd
```

---

## 관계해석 ★A

```mermaid
graph TD
    RC["📝 관계해석 Relational Calculus 053\n관계 데이터의 연산을 표현하는 방법\n코드(E.F.Codd)가 제안\n수학의 술어 해석 Predicate Calculus 기반"]

    RC --> F1["원하는 정보가 무엇인지만 정의\n비절차적 Non-Procedural 특성"]
    RC --> F2["관계대수와 비교\n관계대수 = 절차적 언어\n관계해석 = 비절차적 언어"]

    style RC fill:#e2d9f3
    style F1 fill:#d4edda
    style F2 fill:#cce5ff
```

---

## 접근통제 3가지 ★B

```mermaid
graph TD
    AC["🔐 접근통제 Access Control"]

    AC --> DAC["임의 접근통제\nDAC\nDiscretionary\n데이터 소유자가\n권한 결정\nGRANT/REVOKE"]
    AC --> MAC["강제 접근통제\nMAC\nMandatory\n시스템이 권한 결정\n보안 등급 비교"]
    AC --> RBAC["역할 기반 접근통제\nRBAC\nRole-Based\n역할에 따라\n권한 부여"]

    style DAC fill:#d4edda
    style MAC fill:#f8d7da
    style RBAC fill:#cce5ff
```

---

## 반정규화 ★A

```mermaid
graph TD
    DN["📦 반정규화 De-normalization 059\n정규화된 DB를 성능 향상 목적으로\n의도적으로 중복·통합·분리하는 기법"]

    DN --> W["반정규화 이유\n조인 증가→성능 저하 우려\n조회 속도 향상 필요"]
    DN --> T1["테이블 통합\n조인이 잦은 두 테이블 합치기"]
    DN --> T2["테이블 분리\n자주 사용 컬럼 별도 분리"]
    DN --> T3["중복 테이블 추가\n집계 테이블, 진행 테이블 추가"]
    DN --> C1["컬럼 중복\n조인 없이 조회하도록 컬럼 복사"]
    DN --> C2["파생 컬럼 추가\n집계값 미리 계산해 컬럼에 저장"]

    style DN fill:#fff3cd
    style T1 fill:#d4edda
    style T2 fill:#cce5ff
    style T3 fill:#cce5ff
    style C1 fill:#f8d7da
    style C2 fill:#f8d7da
```

---

## 시스템 카탈로그 ★B

```mermaid
graph TD
    CAT["📁 시스템 카탈로그 System Catalog 060\n= 데이터 딕셔너리 Data Dictionary\n= 메타 데이터 Meta Data\nDBMS가 스스로 관리하는 DB 내부 정보"]

    CAT --> C1["저장 내용\n테이블·뷰·인덱스·권한 정보\n컬럼 타입·제약조건 등"]
    CAT --> C2["사용자는 조회만 가능\n직접 수정 불가\nDDL 실행 시 DBMS가 자동 갱신"]

    style CAT fill:#e2d9f3
    style C1 fill:#d4edda
    style C2 fill:#f8d7da
```

---

## 뷰(View) ★B

```mermaid
graph TD
    VIEW["🪟 뷰 View 064\n하나 이상의 기본 테이블로부터\n유도된 가상 테이블\n실제 데이터 저장 X"]

    VIEW --> CR["CREATE VIEW 뷰이름 AS\n  SELECT 문\n  [WITH CHECK OPTION]"]
    VIEW --> DR["DROP VIEW 뷰이름"]
    VIEW --> A1["뷰의 장점\n독립성·편의성·보안성 제공"]
    VIEW --> A2["뷰의 단점\n인덱스 사용 불가\n삽입·삭제·수정 제한"]

    style VIEW fill:#cce5ff
    style CR fill:#d4edda
    style DR fill:#f8d7da
    style A1 fill:#fff3cd
    style A2 fill:#e2d9f3
```

---

## 파티션의 종류 ★B

```mermaid
graph TD
    PART["🗂️ 파티션 Partition 065\n대용량 테이블을 여러 물리 단위로 나눔\n성능 향상·관리 용이"]

    PART --> RP["범위 분할 Range Partition\n컬럼 값의 범위를 기준으로 분할\n예: 날짜별, 금액별"]
    PART --> HP["해시 분할 Hash Partition\n해시 함수 적용 결과로 분할\n균등 분산에 유리"]
    PART --> LP["목록 분할 List Partition\n지정한 값 목록에 따라 분할\n예: 지역별, 카테고리별"]
    PART --> CP["조합 분할 Composite Partition\n범위+해시 등 두 방식 조합"]

    style PART fill:#fff3cd
    style RP fill:#d4edda
    style HP fill:#cce5ff
    style LP fill:#f8d7da
    style CP fill:#e2d9f3
```

---

## 분산 데이터베이스의 목표 ★B

```mermaid
graph TD
    DIST["🌐 분산 데이터베이스 목표 066\n여러 사이트에 분산된 DB\n사용자에게 단일 DB처럼 보이도록"]

    DIST --> T1["위치 투명성 Location Transparency\n데이터가 어느 사이트에 있는지 몰라도 접근 가능"]
    DIST --> T2["중복 투명성 Replication Transparency\n데이터 복제 여부를 몰라도 사용 가능"]
    DIST --> T3["병행 투명성 Concurrency Transparency\n다수 사용자가 동시 접근해도 일관성 유지"]
    DIST --> T4["장애 투명성 Failure Transparency\n일부 사이트 장애 발생해도 작업 계속 가능"]

    style DIST fill:#e2d9f3
    style T1 fill:#d4edda
    style T2 fill:#cce5ff
    style T3 fill:#fff3cd
    style T4 fill:#f8d7da
```

---

## RTO / RPO ★A

```mermaid
graph LR
    subgraph RECOVER["복구 목표 067"]
        RTO["RTO\nRecovery Time Objective\n목표 복구 시간\n장애 발생 후\n서비스 재개까지\n허용 시간"]
        RPO["RPO\nRecovery Point Objective\n목표 복구 시점\n복구 시 허용되는\n데이터 손실 기준 시점\n(어느 시점까지 복구할 것인가)"]
    end

    style RTO fill:#f8d7da
    style RPO fill:#cce5ff
```

---

## 자료 구조의 분류 ★B

```mermaid
graph TD
    DS["📊 자료 구조 Data Structure 073"]

    DS --> L["선형 구조 Linear\n데이터가 일렬로 나열"]
    DS --> NL["비선형 구조 Non-linear\n데이터가 계층·망형으로 연결"]

    L --> L1["배열 Array\n동일 타입, 연속 메모리\n인덱스로 접근 O(1)"]
    L --> L2["스택 Stack\nLIFO (후입선출)"]
    L --> L3["큐 Queue\nFIFO (선입선출)"]
    L --> L4["덱 Deque\n양쪽 끝 삽입·삭제 가능"]

    NL --> NL1["트리 Tree\n계층 구조\n사이클 없음"]
    NL --> NL2["그래프 Graph\n망형 구조\n사이클 가능"]

    style DS fill:#e2d9f3
    style L fill:#d4edda
    style NL fill:#cce5ff
    style L2 fill:#fff3cd
    style NL1 fill:#fff3cd
```

---

## 스택(Stack) ★B

```mermaid
graph TD
    ST["📚 스택 Stack 074\nLIFO (Last In First Out)\n후입선출\n한쪽 끝에서만 삽입·삭제"]

    ST --> OP["주요 연산\nPUSH: 데이터 삽입 (top+1)\nPOP: 데이터 삭제 (top-1)\nPEEK/TOP: 스택 top 확인"]
    ST --> OV["오버플로 Overflow\n스택이 꽉 찬 상태에서 PUSH 시도"]
    ST --> UN["언더플로 Underflow\n스택이 빈 상태에서 POP 시도"]
    ST --> USE["활용 예\n함수 호출 스택\n수식 괄호 검사\n역순 문자열 출력"]

    style ST fill:#fff3cd
    style OP fill:#d4edda
    style OV fill:#f8d7da
    style UN fill:#f8d7da
    style USE fill:#cce5ff
```

---

## 트리 관련 용어 ★B

```mermaid
graph TD
    TREE["🌳 트리 Tree 076\n계층 구조, 사이클 없는 그래프"]

    TREE --> T1["노드 Node\n트리의 기본 요소"]
    TREE --> T2["루트 Root\n최상위 노드 (부모 없음)"]
    TREE --> T3["단말 노드 Leaf Node\n자식 없는 노드 = 단풍잎"]
    TREE --> T4["디그리 Degree\n노드의 자식 수"]
    TREE --> T5["깊이 Depth / 레벨 Level\n루트에서 해당 노드까지 거리"]
    TREE --> T6["이진 트리 Binary Tree\n각 노드의 자식이 최대 2개"]

    style TREE fill:#d4edda
    style T2 fill:#fff3cd
    style T3 fill:#cce5ff
    style T6 fill:#f8d7da
```

---

## 트리 순회 (Preorder / Inorder / Postorder) ★B

```mermaid
graph TD
    TRAV["🔄 트리 운행법 Tree Traversal 077-078"]

    TRAV --> PRE["Preorder 전위 순회 077\nRoot → Left → Right\n루트를 먼저 방문"]
    TRAV --> IN["Inorder 중위 순회 078\nLeft → Root → Right\n루트를 중간에 방문\n이진탐색트리: 오름차순 출력"]
    TRAV --> POST["Postorder 후위 순회\nLeft → Right → Root\n루트를 마지막에 방문"]

    style PRE fill:#d4edda
    style IN fill:#cce5ff
    style POST fill:#fff3cd
```

```
          A
         / \
        B   C
       / \
      D   E

Preorder : A B D E C  (Root→L→R)
Inorder  : D B E A C  (L→Root→R)
Postorder: D E B C A  (L→R→Root)
```

---

## 정렬 알고리즘 ★B

```mermaid
graph TD
    SORT["🔢 정렬 Sort 082-084"]

    SORT --> SEL["선택 정렬 Selection Sort 082\nO(n²)\n전체에서 최솟값 선택 → 맨 앞과 교환\n비교 횟수 일정"]
    SORT --> BUB["버블 정렬 Bubble Sort 083\nO(n²)\n인접한 두 원소 비교 후 교환\n구현 단순, 속도 느림"]
    SORT --> QK["퀵 정렬 Quick Sort 084\n평균 O(n log n)\n최악 O(n²) (이미 정렬된 경우)\n피벗 기준 분할 정복\n실제로 가장 빠름"]

    style SORT fill:#e2d9f3
    style SEL fill:#fff3cd
    style BUB fill:#d4edda
    style QK fill:#cce5ff
```

| 정렬 | 최선 | 평균 | 최악 | 특징 |
|------|------|------|------|------|
| 선택 | O(n²) | O(n²) | O(n²) | 비교 횟수 고정 |
| 버블 | O(n) | O(n²) | O(n²) | 구현 단순 |
| 퀵 | O(n log n) | O(n log n) | O(n²) | 평균 가장 빠름 |
| 병합 | O(n log n) | O(n log n) | O(n log n) | 안정 정렬 |

---

## 핵심 암기 요약표

| 번호 | 항목 | 핵심 키워드 | 난이도 |
|------|------|-------------|--------|
| 034 | 개체 무결성 | 기본키 = NULL 불가, 중복 불가 | **A** |
| 035 | 참조 무결성 | 외래키 = 참조 기본키값 or NULL | **A** |
| 036 | 개념적 설계 | ERD 작성 단계 | **A** |
| 037 | 논리적 설계 | 릴레이션 스키마 변환 | **A** |
| 038 | 물리적 설계 | 저장구조·접근경로 설계 | **A** |
| 039 | 데이터 모델 3요소 | 구조·연산·제약조건 | **A** |
| 040 | E-R 기호 | 사각형=개체/타원=속성/밑줄타원=기본키/마름모=관계 | **B** |
| 041 | 튜플/카디널리티 | 행(Row), 튜플 수=카디널리티 | **A** |
| 042 | 속성/디그리 | 열(Column), 속성 수=디그리 | **A** |
| 043 | 도메인 | 속성이 가질 수 있는 값의 범위 | **A** |
| 044 | 후보키 | 유일성+최소성 모두 만족 | **A** |
| 045 | 기본키 | 후보키 중 선택, NULL 불가 | **A** |
| 046 | 대체키 | 후보키 중 기본키 외 나머지 (=보조키) | **A** |
| 047 | 슈퍼키 | 유일성O 최소성X | **A** |
| 048 | 외래키 | 다른 릴레이션의 기본키 참조 | **A** |
| 050 | 이상(Anomaly) | 삽입·삭제·갱신 이상 | **A** |
| 052 | 일반 집합 연산자 | ∪합집합 ∩교집합 −차집합 ×교차곱 | **A** |
| 053 | 관계해석 | 비절차적, 원하는 결과만 기술 | **A** |
| 055 | 함수적 종속 | 완전·부분·이행적 함수종속 | **A** |
| 056 | 1NF | 원자값만 허용 | **A** |
| 057 | 2NF | 부분 함수 종속 제거 | **A** |
| 058 | 3NF/BCNF | 이행 종속 제거 / 결정자=후보키 | **B** |
| 059 | 반정규화 | 성능 향상, 의도적 중복·통합·분리 | **A** |
| 060 | 시스템 카탈로그 | 데이터 딕셔너리, 메타데이터 | **B** |
| 064 | 뷰(View) | 가상 테이블, CREATE VIEW AS SELECT | **B** |
| 065 | 파티션 종류 | 범위·해시·목록·조합 분할 | **B** |
| 066 | 분산DB 목표 | 위치·중복·병행·장애 투명성 | **B** |
| 067 | RTO/RPO | 목표복구시간 / 목표복구시점 | **A** |
| 073 | 자료 구조 분류 | 선형(배열·스택·큐·덱) 비선형(트리·그래프) | **B** |
| 074 | 스택 | LIFO, 오버플로/언더플로 | **B** |
| 076 | 트리 용어 | 노드·루트·단말·디그리·깊이 | **B** |
| 077 | Preorder | Root→Left→Right | **B** |
| 078 | Inorder | Left→Root→Right | **B** |
| 082 | 선택 정렬 | O(n²), 최솟값 찾아 교환 | **B** |
| 083 | 버블 정렬 | O(n²), 인접 비교 교환 | **B** |
| 084 | 퀵 정렬 | 평균 O(n log n), 피벗 분할 | **B** |

---

*2장 데이터 입출력 구현 (실기_이론(1) p.3~4 기반)*
